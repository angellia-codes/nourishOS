{
  "name": "nourishos-apps-script",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "login": "clasp login",
    "create": "clasp create --type webapp --title \"NourishOS API\" --rootDir src",
    "push": "clasp push",
    "deploy": "clasp deploy",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
