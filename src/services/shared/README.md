# shared

Client wrappers for the mandatory Shared Services: approvalService, taskService, notificationService, auditService (read-only), fileService, searchService. Every feature service composes these instead of reimplementing workflow/audit/notification logic.
