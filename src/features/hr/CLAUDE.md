# HR module — current state

Migrated out of the root `CLAUDE.md` (2026-08-15) so this detail only loads when you're actually working under `src/features/hr/` or `functions/src/hr/`. Same "facts, not standards" rule applies — if your change alters a fact here, update it in the same commit. The root file still holds cross-cutting architecture, RBAC, and Definition of Done.

- **`Employee` in `src/types/employee.types.ts` is the shipped shape** (mirrors what the old, now-unported `createEmployee` action used to write: `position`, `employmentStatus`, ISO-string dates). The PRD §12.1 schema (`TaxStatus`, `EmployeeCompensation`, etc.) sits below it in a clearly-marked PLANNED section — do not use those in shipped code paths yet.
- **`Employee.position` moved from free text to a validated `PositionId` (2026-08-17), closing the gap root `CLAUDE.md`'s "Departments > People" note flagged.** `POSITIONS.md` §3/§4's catalog already existed client-side as `POSITION_CATALOG`/`POSITION_LABELS` (`src/constants/positions.ts`) but nothing validated against it. New: `positionsForDepartment(departmentId)` scopes the catalog per app department (`organization.ts`'s `admin_general`/`cashier`/etc.) via a hand-curated `DEPARTMENT_POSITION_IDS` map — **not** a mechanical join on `POSITION_CATALOG[id].department`, since POSITIONS.md's own department grouping (`fbService`/`finance`/`bar`/…) is a different taxonomy that doesn't line up 1:1 with the app's operational departments. Mirrored server-side in `functions/src/lib/positions.ts` (same convention as `organization.ts`) — `createEmployee`/`updateEmployee` now reject a position outside the department's allow-list, re-checked on `updateEmployee` whenever either `position` or `departmentId` changes (a department transfer alone can orphan a previously-valid position). `EmployeeFormPage.tsx`'s Position `<Select>` follows the exact legacy-value-fallback pattern already established for Department/Outlet/Religion. Every place `Employee.position` is displayed (list/profile/exports/reports) resolves through `POSITION_LABELS[position] ?? position`, and `EmployeeImportPage.tsx`'s CSV importer resolves Position the same way it already resolved Gender/Employment Status/Contract Type (`resolveEnum` against value-or-label), scoped to the row's resolved department. This is what makes `probationReviewTrigger.ts`'s `positionId: employee.position` actually match `AppraisalTemplateSeed.positionId` for employees hired going forward — legacy employees and positions with no seeded probation template are the two remaining reasons that trigger still falls back to notifying `hrManager` instead of creating the review.
- **`DEPARTMENT_POSITION_IDS` revised the same day against an explicit 12-point per-department policy**, replacing the first pass's "every catalog entry under its POSITIONS.md heading" default. Specifics worth knowing:
  - **`trainee`/`dailyWorker` are no longer appended to every department** — they're listed explicitly, and only, under `fb_service`/`bar`/`kitchen`. ("The Bakery Kitchen" is an outlet, not a department — `OUTLET_DEPARTMENTS` staffs it from `kitchen`.) `central_kitchen`/`security`/`wholefood_retail` weren't named by the policy and keep their original (non-trainee/DW) lists unchanged.
  - **`housekeeping` is deliberately empty — confirmed with the user, not a bug.** POSITIONS.md has no Housekeeping section and the policy didn't add trainee/dailyWorker back for it, so **no position is currently selectable for that department**; registering an employee into Housekeeping is blocked until real titles exist in the taxonomy.
  - `purchasingManager`/`purchasingSupervisor` moved from `driver` to `finance_accounting`; `driver` is now exactly Driver Leader/Driver.
  - `restaurantMaintenanceManager` is now offered from **both** `fb_service` and `engineering_pomec` — the policy added it to the former without removing it from the latter.
  - Several catalog ids became unreachable from *every* department (`ceo`, `groupOperationalManager`, `operationalManager`, `runner`, `groupBarManager`, `barBack`, `assistantBarManager`, `directorOfSalesMarketing`, `socialMediaSpecialist`, `engineerCivil`, `groupHrManager`, `trainingDevelopmentSupervisor`, `groupFinancialController`, `costControl`, `arIncomeAudit`) — left in `POSITION_CATALOG`/`POSITION_LABELS` rather than deleted, since a legacy employee record may still carry one and needs it to keep resolving to a label.
- **Position scoping gained an outlet dimension the same day: `positionsForDepartment(departmentId)` → `positionsFor(outletId, departmentId)`.** `kitchen` is staffed by both `the_bakery_kitchen` and the standard restaurant outlets (`nourish_ungasan`/`nourish_uluwatu`/`nourish_berawa`), and the two outlet types split cleanly along a bakery/restaurant line — a department-only lookup can't express "only at this outlet within the department," so `OUTLET_ONLY_POSITION_IDS` (both `src/constants/positions.ts` and `functions/src/lib/positions.ts`) intersects the department list per position: **bakery-only** (Chief Baker, Chef de Partie Baker/Pastry, Cook/Baker → `the_bakery_kitchen`) and **restaurant-only** (Head Chef, Sous Chef, Chef de Partie, Demi Chef de Partie, Cook, Cook Helper → the three `nourish_*` outlets, via a shared `STANDARD_RESTAURANT_OUTLET_IDS`). `steward`/`trainee`/`dailyWorker` are deliberately unrestricted — common to both outlet types. Every call site threads outletId alongside departmentId: `EmployeeFormPage.tsx`'s Position `<Select>` (also resets on outlet change, not just department change), `EmployeeImportPage.tsx`'s CSV resolver, and `createEmployee`/`updateEmployee`'s server-side validation (`updateEmployee` re-checks position whenever `position`, `departmentId`, **or `outletId`** changes — an outlet transfer within the same department, e.g. off `the_bakery_kitchen`, can orphan a bakery- or restaurant-only title the same way a department transfer can).
- **Employee create/edit revised (2026-08-15): `preferredName` removed, Department/Outlet are real dropdowns, a Contract PDF slot, and bulk import.** `preferredName` is gone everywhere (type, both callables, form, CSV export, search, profile) — it had no other logic depending on it. Department/Outlet on `EmployeeFormPage.tsx` were plain free-text `<Input>`s (typo-prone, and `departmentId` is what `firestore.rules` uses to scope department-leader reads) — they're now cascading `<Select>`s mirroring `RegisterPage.tsx`'s `OUTLETS`/`OUTLET_DEPARTMENTS`/`optionsFor` pattern, with a legacy-value fallback so an existing bad value stays editable instead of silently blanking. `createEmployee.ts`/`updateEmployee.ts` now validate the pair against `OUTLET_DEPARTMENTS` server-side too — closing a gap `registerUser.ts` already covered but the employee callables never did. `EmployeeProfilePage.tsx` gained a distinct "Contract Document" card (`resourceType: 'employeeContract'`, PDF-only) alongside the pre-existing generic "Documents" card (`resourceType: 'employee'`) — same `FileUpload`/`FileList`/`createFileMetadata`/`deleteFile` mechanism, zero backend changes, since `resourceType` is just a string and `storage.rules`/`firestore.rules` are already resourceType-agnostic. **Bulk import** (`/hr/employees/import`, HR_OPERATIONS.md §9.1-F12, previously unbuilt anywhere in the app) follows the one existing precedent for reusing a callable's mutation logic from a second caller — `createCalendarEventInternal`'s extraction — so `createEmployee.ts` now exports `createEmployeeInternal`, and the new `importEmployees` callable loops it per row with partial-success semantics (a bad row is reported, not thrown, and the batch continues), capped at 100 rows. No permission-string mirror was duplicated for this: `src/utils/csv.ts` gained a hand-rolled `parseCsv` (no CSV library on either side, still none — this reads a template the app itself generates) and the import page resolves Department/Outlet/Gender/Employment Status/Contract Type from either their display label or raw value, since the template is filled in by hand.
- **The HR recruitment pipeline is shipped end to end (2026-08-14), and `/hr` is now a hub.** The employee register moved from the `/hr` index to `/hr/employees`; the index is [src/features/hr/pages/HrHomePage.tsx](src/features/hr/pages/HrHomePage.tsx). Four chained sub-modules live under [src/features/hr/recruitment/](src/features/hr/recruitment/) and [functions/src/hr/recruitment/](functions/src/hr/recruitment/): requisitions (`recruitments`), candidates (`candidates`, stages ST-01…ST-08), interviews (`interviews`), onboarding (`onboardingChecklists`) — twelve callables plus an `hr/requisition` approval route. Things worth knowing before touching it: **a candidate cannot exist without an approved requisition** and **`vacancyStage` is only ever set by the approval-resolved handler** in `functions/src/hr/recruitment/index.ts`, which is what makes "no vacancy before approval" true rather than merely intended; hiring (ST-06) is the one write with side effects — it counts against the requisition inside a transaction, completes it when the last opening is filled, and generates the onboarding checklist; **`createCalendarEventInternal` was extracted** from `functions/src/shared/calendar/events.ts` so `scheduleInterview` can write a calendar event without a second callable (the callable now owns only auth + audit); `createEmployee` gained an optional `candidateId` that links the new employee back to the candidate and its checklist, since **hiring deliberately does not auto-create an employee** (candidate data has no NIK, contract type or probation). Deviations from `docs/modules/employee-requisition.md` and `HR_OPERATIONS.md` §9.4: the approval chain is fixed (hrManager → generalManager) rather than adding a Director step for unbudgeted requests — `budgeted` is captured, so that is a `shared/approval/routes.ts` change when wanted; no `confidential/compensation` subdocument; no WhatsApp (§9.5) since no Fonnte adapter exists; the board moves stages via a `<Select>` rather than drag-and-drop (no new dependency). `ROLE_PERMISSIONS` gained `recruitment.read`/`recruitment.create` for the leader roles — **that only affects role docs seeded from now on**, so existing `roles/{roleId}` documents need the strings added by hand.
- **HR Inventory ships uniform & asset stock tracking at `/hr/inventory` (2026-08-15), a 5th card on the HR hub.** `docs/modules/hr.md` §12 "Employee Assets" names `employeeAssets` and an Assign/Transfer/Return-per-asset shape; this ships a **unified stock ledger** instead — item master + per-outlet (per-size, when the item tracks sizes) stock levels + an append-only movement log — because uniforms need size-level quantities a per-asset-unit model doesn't represent, and "stock tracking, movement, cost" is the ledger pattern, not an assign/return log. **`EMPLOYEE_ASSETS` stays reserved and unused.** Deliberately distinct collections — `hrInventoryItems`/`hrStockLevels`/`hrStockMovements` in [functions/src/hr/inventory/](functions/src/hr/inventory/) — from the `INVENTORY_ITEMS`/`STOCK_LEVELS`/`STOCK_MOVEMENTS`/etc. that were reserved in `src/constants/collections.ts` for a separate, much larger F&B warehouse module `docs/modules/inventory.md` used to describe (FIFO/FEFO, waste logs). **That top-level module — and Purchasing and CRM alongside it — was cut from scope on 2026-08-15**: the reserved collection names, the `/inventory`, `/purchasing` and `/crm` `<ModulePlaceholder>` routes, their `Sidebar`/`ROUTES` entries, and all three PRD docs were deleted; HR Inventory is now the only inventory concept in the app. Five callables (`createInventoryItem`, `updateInventoryItem`, `receiveStock`, `issueStock`, `transferStock`) share one transactional helper, `applyDelta` (pinned by `node functions/test/hr-inventory-stock.mjs`, no emulator needed) — every mutation runs inside `db.runTransaction`, and `transferStock` reads both outlets' stock levels before writing either (Firestore transactions forbid reads after writes), writing two linked movement docs (`transferOut`/`transferIn`) sharing a `linkedMovementId`. `category` and `hasSizes` are immutable after item creation — changing either would orphan existing `hrStockLevels` docs keyed by the old size set. No per-serial asset tracking (a laptop is a count of "laptop" stock, not an individually tagged unit) and no approval engine — permission-gated like Operations, not multi-step approved like Finance. New permissions `hrInventory.manage` (item-master curation, `hrManager` only) and `hrInventory.record` (day-to-day receive/issue/transfer, granted to `hrManager` and every outlet leader via the shared `LEADER` array) — same standing caveat as every prior module: `ROLE_PERMISSIONS` only seeds a role doc on first claim, so existing `roles/{roleId}` docs (including `roles/superAdmin`) need both strings added by hand.
- **HR ships a Reports sub-module at `/hr/reports` (2026-08-15), a 6th card on the HR hub — eight report views, zero backend changes.** Active Employee, Resigned Employee, Employee Turnover, Manning Budget, Manning Cost, Employee Activity, Budget and Cost, and Upcoming Activity and Budget (`docs/modules/hr.md` §16 names most of these as aspirational; Manning Budget/Cost/Budget-and-Cost/Upcoming are net-new, not in that list). Every report is a client-side aggregation over collections the HR sub-modules already write (`employees`, `recruitments`, `interviews`, `onboardingChecklists`, `employeeActivities`, `hrStockMovements`) via new unfiltered/lightly-filtered subscriptions added alongside the existing ones (`subscribeToAllEmployeeActivities`, `subscribeToAllStockMovements`, `subscribeToAllInterviews`) — same "single subscription, filter client-side" convention `subscribeToEmployees`/`subscribeToAllStockLevels` already use, so no new Firestore indexes. **No new permission string** — a report is a different view over data the viewer's existing role can already read via `firestore.rules`, so there's no `hrReports.read`; pages that read `recruitments`/`interviews` (Manning Budget, Budget and Cost, Upcoming Activity and Budget) surface the same role-gated "Access restricted" state `RequisitionListPage` already does when that subscription errors. The one real gap (now partially closed, see the §12.1 compensation entry below): **Manning Cost and Budget and Cost are uniform/asset procurement spend from `hrStockMovements.totalCost`, not payroll** — these reports were never wired to `EmployeeCompensation` and still aren't, a deliberate scope call, not an oversight, confirmed before building. Shared additions worth knowing about elsewhere: `src/utils/csv.ts` (`toCsv`/`downloadCsv`) is now the one CSV serializer — `employeeExport.ts` was refactored to call it instead of hand-rolling escaping — and `src/components/shared/ReportTable.tsx` is a minimal column-config table (no sorting/pagination built in, filtering stays page-local) available to any future report-shaped page.
- **HR_OPERATIONS.md gap-closing pass (2026-08-15): employee polish, recruitment analytics, calendar improvements, and a Contract/Probation Tracker.** (Calendar improvements moved to `src/features/calendar/CLAUDE.md`.) A full read of `docs/core/HR_OPERATIONS.md` against the codebase found its §16 roadmap stale (it describes Calendar/Daily Updates/Operations as unbuilt; all three had shipped) — the per-requirement `9.x-Fxx` tables cross-referenced against code were used instead. Out of scope at the time, flagged rather than built: WhatsApp/Fonnte, Google Calendar sync, the §6.4 weighted-component appraisal scoring model, and Disciplinary Actions/Training as full modules (the doc doesn't actually spec either beyond a reserved collection name). **WhatsApp and Google Calendar sync both shipped on 2026-08-18** once the accounts existed (see the last two bullets here and `src/features/calendar/CLAUDE.md`); **§6.4 remains the one open item** — it is a product decision (Option A vs B) the PRD itself flags, not an engineering gap, so `AppraisalReviewType` is still `probation | quarterly | annual` with a plain average `overallScore`, no `scoringModel` field, and no contractRenewal/PIP review types.
  - **Employee polish**: `preferredName`-adjacent gaps closed — birth-month/religion filters and a birth-date sort on `EmployeeListPage.tsx`/`employeeSort.ts`; `disciplinaryType`/`disciplinaryStartPeriod`/`disciplinaryEndPeriod`/`recognitionType`/`recognitionPeriod` added to `Employee` (`DisciplinaryType` promoted out of `employee.types.ts`'s PLANNED section into `src/constants/hr.ts`, mirrored in `functions/src/hr/employees/helpers.ts`); a "Change History" card on `EmployeeProfilePage.tsx`. That last one needed a new callable, not a direct Firestore read — `auditLogs` is `allow read, write: if false` in `firestore.rules`, closed even to elevated roles per `AUDIT_LOG.md` §8, so [functions/src/hr/employees/getEmployeeAuditLog.ts](functions/src/hr/employees/getEmployeeAuditLog.ts) is the one narrow read-only exception in the app to "clients read Firestore directly" — gated by `EMPLOYEES_UPDATE`, returns entries, never mutates. New composite index: `auditLogs` resourceType+resourceId+timestamp.
  - **Recruitment analytics**: `timeToHireDays()` in `recruitmentFormat.ts` (derived from `stageHistory`, same style as the existing `daysInStage()`), shown on `CandidateDetailPage.tsx` for hired candidates. A 9th HR Report, **Recruitment Funnel** (`/hr/reports/recruitment-funnel`), follows the other eight's shape exactly — client-side aggregation, no backend changes — plus a "Print / Save as PDF" button using `window.print()` and a `print:` Tailwind variant rather than a new PDF library (none exists anywhere in the app, and browser print-to-PDF covers "export to PDF" natively).
  - **Contract/Probation Tracker** (the WhatsApp delivery leg was added 2026-08-18, see the WhatsApp bullet at the end): [functions/src/hr/employees/contractAlerts.ts](functions/src/hr/employees/contractAlerts.ts), a new `onSchedule` job (06:00 WITA) implementing §14.1's threshold algorithm — contract expiry at 90/60/30/14/7/0 days, probation ending at 30/14/7 — via an exact-day-match Firestore query (`where(field,'==',targetDate)`, two equality filters, no new index needed) rather than fetching every employee and computing date diffs. **Deliberately stateless**: no `sentFlags` dedup map like the doc's own spec, since each threshold day matches exactly once as time passes — a real, noted simplification, not an oversight. Recipients escalate with `notifyUsersByRole`: `hrManager` always, the employee's own department leader (`DEPARTMENT_ROLES[departmentId][0]`, same lookup Finance's `expenseSteps.ts` uses) from 30 days out, `generalManager` from 7 days out.
- **Disciplinary Records** (FEATURE_SPECIFICATIONS.md pass, 2026-08-15): reverses a prior deliberate scope-down — `HR_OPERATIONS.md` only asked for the 3 status fields already on `Employee`, but `FEATURE_SPECIFICATIONS.md` names Warning/Suspension Records and Investigation Notes as their own feature. `disciplinaryActions` (reserved, previously unused) is now the detail layer behind those fields — additive, **no auto-sync** back onto `Employee.disciplinaryType`/Start/End (HR sets both independently, a deliberate simplification). Investigation notes are an append-only array (`FieldValue.arrayUnion`, timestamped client-side since `serverTimestamp()` can't sit inside `arrayUnion`). Gated by the existing `employees.update` — no new permission string. Read is HR/elevated-only in `firestore.rules`, intentionally without the department-leader branch `/employees` has (would need a `get()` to the employee doc for `departmentId`, and this is more sensitive than the base record). A Communications sub-module for issuing a formal per-employee notice tied to a Disciplinary Record is explicitly deferred — scope not yet decided (see `src/features/communications/CLAUDE.md`).
- **`Departments > People` shipped 2026-08-16 and was deleted the same day** (root `CLAUDE.md` has the full writeup) — `EmployeeListPage.tsx` is back to its pre-module shape, no `lockActiveStatus` prop. One backend artifact from that module survives: `createAppraisal.ts` was split into `createAppraisalInternal` (also takes an optional `reviewerId`, defaulting to the caller's own uid same as before) plus the thin `onCall` wrapper, so `triggerProbationReviews` (a scheduled function, still shipped) can create a probation appraisal with no human caller.
- **Religion revision (2026-08-15)**: Employee `religion` was a free-text `<Input>` on both client and server — it's now a real enum, `RELIGION`/`RELIGION_LABELS` in `src/constants/hr.ts` (mirrored `RELIGIONS` in `functions/src/hr/employees/helpers.ts`): `hindu`/`christian`/`catholic`/`islam`/`other`, the 5 values actually wanted (a first pass shipped Indonesia's fuller 6-religion KTP list, corrected down to this set same-day). The incomplete `Religion = 'hindu' | 'christian' | 'muslim'` PLANNED type in `employee.types.ts` is gone, superseded by this. `EmployeeFormPage.tsx`'s religion `<Select>` carries the same legacy-value fallback the Department/Outlet dropdowns already established (an existing free-text value, or a value from the first-pass 6-religion list, stays selectable rather than blanking); `EmployeeImportPage.tsx`'s CSV resolver and `EmployeeListPage.tsx`'s filter both moved onto the fixed enum too.
- **HR.md §9/§11/§13 gap-closing pass (2026-08-16): Employment Contracts module, Training module, and Employee Activities logging closed up.** `recordEmployeeActivity` (`functions/src/hr/employees/helpers.ts`) previously only fired on hire/update/archive — two more real events now log too: disciplinary warnings (`disciplinaryRecords.ts`) and appraisal completion (the `hr/appraisal` approval-resolved handler, via a synthetic `SYSTEM_USER` since approval resolution has no human caller, same shape as `probationReviewTrigger.ts`'s). `updateEmployee.ts` also branches its activity description by which field actually changed (`position` → `promoted`, `departmentId` → `departmentTransfer`, `outletId` → `outletTransfer`) instead of always logging a generic `updated`.
  - **Employment Contracts** (`functions/src/hr/contracts/`, `src/features/hr/contracts/`): a new `contracts` collection holds one row per contract *version* per employee — append-only history behind `Employee.contractType/contractStartDate/contractEndDate`, which stay as a denormalized "current contract" cache that `renewContract`/`terminateContract` keep in lockstep (existing readers — the employee list/filter/export, `contractAlerts.ts` — are untouched). `createContractInternal` writes the version-1 row from inside `createEmployeeInternal`. An "extend" per the doc is just `renewContract` with the same `contractType` and a later end date — no separate callable. `terminateContract` deliberately does **not** touch `Employee.status`; ending employment stays on the existing `archiveEmployee` flow (confirmed product decision — a terminated contract isn't necessarily a departure). Gated by the existing `employees.update`, same precedent as Disciplinary Records — no new permission string. Reached from the employee profile's new "Contract History" card, not a HR-hub card (employee-scoped, like Disciplinary).
  - **Training** (`functions/src/hr/training/`, `src/features/hr/training/`, `/hr/training`): a 7th HR-hub card. Two collections — `trainings` (catalog, open read like `hrInventoryItems`) and net-new `trainingAssignments` (per-employee join, `departmentId` denormalized at assign time so a leader's `firestore.rules` scope matches HR.md §22's "Limited" row). Claims the `training.read`/`training.assign` permission strings that were reserved and pre-seeded on `LEADER`/`hrManager` in `ROLE_PERMISSIONS` since HR Inventory shipped but never consumed — no `ROLE_PERMISSIONS` edit needed, only the `functions/src/lib/permissions.ts` mirror (client side already had them). Certificates use the existing generic `FileUpload`/`FileList` pattern (`resourceType: 'trainingCertificate'`, `resourceId`: the assignment id) — zero file-storage backend changes, same mechanism as the Contract Document card. No notification on assignment: `Employee` has no linked auth-uid field to notify against (checked — `appraisals`' self-read rule keys off `employeeId == request.auth.uid`, implying appraisal subjects are addressed by uid directly, not something `Employee` exposes), a known limitation rather than an oversight, flagged the same way `contractAlerts.ts` flags its missing WhatsApp leg.
- **Requisition gap-closing pass against employee-requisition.md (2026-08-17).** The module was already fully shipped; this closed several confirmed gaps beyond the three already-known deviations (still no WhatsApp; approval chain still fixed hrManager→generalManager — see next bullet). New: a **Compensation subdocument** (`recruitments/{id}/confidential/compensation`, `RequisitionCompensation` type, `updateRequisitionCompensation` callable, gated by new permission `recruitment.viewCompensation` — granted to `hrManager`/`generalManager`/`director` in `ROLE_PERMISSIONS`, same field-level-security reasoning as `employees/{id}/compensation/current`), shown on `RequisitionDetailPage.tsx`'s new Compensation card, inline-edit like the Employee profile's. **Attachments** — a new "Attachments" card on `RequisitionDetailPage.tsx` using the existing generic `FileUpload`/`FileList` pattern (`resourceType: 'requisitionAttachment'`), zero backend changes. **Position field** on `RequisitionFormPage.tsx` is now a `<Select>` sourced from `positionsFor(outletId, departmentId)` (same catalog/legacy-fallback pattern `EmployeeFormPage.tsx` uses) instead of free text, validated server-side in `requisitions.ts`'s `validateFields` (now `async`). **Replacing Employee** is now a `<Select>` of active employees in the requisition's own department (`listActiveEmployeesInDepartment` — scoped to department because `firestore.rules` only lets a leader read their own department's employees), validated server-side against a real active `employees` doc. **Cancelling an approved requisition with active candidates now notifies interviewers** with a still-`pending` interview against one of those candidates (`cancelRequisition`, queries `interviews` by the same candidate-id list the confirmation gate already collects). **`approvedAt`/`completedAt`** are now typed fields on `Requisition` — `approvedAt` was already being written by the approval-resolved handler (just untyped before); `completedAt` is new, set inside `recordHire()`'s transaction (`candidates.ts`) alongside the **previously-missing `RequisitionCompleted` audit event** for the `approved → completed` transition (a real correctness gap found during the audit, not a scope call). Powers a new **Open Positions dashboard widget** (`src/features/dashboard/widgets/OpenPositionsWidget.tsx`, approved+open-vacancy requisitions) and a new **"Time to Fill — by Requisition"** table on the Recruitment Funnel Report (`timeToFillDays()` in `recruitmentFormat.ts`, requisition-level, distinct from the existing per-candidate `timeToHireDays()`). New composite indexes: `employees` status+departmentId+fullName, `interviews` candidateId+outcome.
- **Explicitly confirmed NOT a gap, permanently: the Director conditional-approval step for unbudgeted requisitions (employee-requisition.md §5/§10 criterion 2).** User-confirmed product decision during the same pass above — the approval chain stays `hrManager → generalManager` unconditionally; Director keeps read-only access to requisitions regardless of `budgeted`. `routes.ts`'s comment on `'hr/requisition'` and `requisitions.ts`'s module header were both corrected to state this plainly rather than frame it as an open TODO (an earlier comment had implied it was still pending).
- **Offboarding & Exit Interview shipped (2026-08-17): the exit/clearance half of `employee-onboarding-exit-checklist.md` §5, plus the full F009 structured exit interview (`exit-interview.md`).** Onboarding's §4 (IN list) was already fully built — this closes the doc's own noted gap ("Not built here: the OUT/exit checklist" in `onboarding.ts`'s header). Mirrors onboarding's architecture exactly: `offboardingChecklists` holds a 3-item embedded `documentChecklist` (Resignation Letter, Out Photo, Bermaterai statement) plus `taskIds` for 6–7 real Task Engine tasks (Asset Return, Document Handover, conditionally Task/Work Reassignment Review, Final Settlement, Exit Interview, Certificate/Reference Letter, BPJS-TK Closure). Triggered from `archiveEmployee` (`functions/src/hr/employees/offboarding.ts`'s `createOffboardingChecklistInternal`), which also gained a required `lastWorkingDate` input (distinct from `resignationDate` — when the resignation was recorded vs. the actual final day) and now returns `offboardingChecklistId`. `updateOffboardingItem`/`completeOffboarding` are gated by the existing `employees.update`, same precedent as Contracts/Disciplinary Records — no new permission string for the checklist itself. Reached via a new 8th HR-hub card (`/hr/offboarding`) and a "View offboarding checklist" link on the employee profile's Separation card.
  - **Handover List gate (§5 item 8) has no data to check against**: no position-tier/"supervisor level" field exists anywhere in the codebase. Ships as a hand-curated heuristic (user-confirmed) in `offboarding.ts` — HQ backoffice departments always qualify, every other department qualifies unless the position is on a short rank-and-file list. Revise the two arrays there directly if the real policy differs.
  - **Clearance Statement (§5 item 2) is a client-rendered print page**, not a server-generated stored PDF — `ClearanceStatementPage.tsx` uses `window.print()`, same convention `RecruitmentFunnelReportPage.tsx` already established (still no PDF library anywhere in this app). No `clearanceStatementFileId` field.
  - **Exit Interview is the full F009 survey**, not just a notes-on-a-task placeholder: new `exitInterviews` collection (`functions/src/hr/employees/exitInterview.ts`'s `submitExitInterview`), gated by new permission `exitInterviews.view` (hrManager/superAdmin only — the doc's own harder confidentiality wall, since Section I asks the employee to rate their manager; granted only to `hrManager` in `ROLE_PERMISSIONS`, so existing `roles/hrManager` docs need it added by hand, same standing caveat as every prior permission addition here). Submitting auto-completes the "Exit Interview" task via a new `completeTaskInternal` extracted from `functions/src/shared/tasks/tasks.ts`'s `completeTask` (needed a new composite index: `tasks` `referenceId`+`tags array-contains`). **Sections H/I/J's 32 rating items (`src/constants/exitInterview.ts`) are placeholder wording** — the source F009 docx wasn't available when this shipped (user-confirmed); swapping in the real bilingual text is a data-only change, no schema/code touch. Aggregate reporting (§6, including the manager-rating N≥3 safeguard) is a new `getExitInterviewInsights` callable rather than a direct-read report like the other 9 HR reports — GM/Director get rollups through it since raw `exitInterviews` rows stay hrManager/superAdmin-only in `firestore.rules`, and the N≥3 gate has to be enforced server-side. Surfaced as a 10th HR Report, `/hr/reports/exit-interview-insights`.
- **WhatsApp shipped 2026-08-18 (HR_OPERATIONS.md §9.5/§9.11/§13.1) — the Fonnte adapter every prior pass flagged as blocked now exists.** [functions/src/shared/notifications/whatsapp.ts](functions/src/shared/notifications/whatsapp.ts) is a plain `fetch` POST to `api.fonnte.com/send` with §13.1's 3-retries/5s-backoff — **no SDK, no new dependency**. One deliberate deviation from §13.1's table: the doc writes `Authorization: Bearer {token}`, but the live Fonnte API takes the bare token with no scheme, and the code follows the API. It sits *behind* the existing Notification Engine per §6.2's "do not build a second notification log": `sendNotificationInternal` gained one optional `whatsapp?: true` flag (off by default, so all ~40 existing call sites are untouched) and `notifyUsersByRole` passes it through. The in-app doc is written **first and never rolled back** — it's the durable record, WhatsApp is best-effort on top, and §13.1's delivery-status block (`whatsappStatus`/`whatsappMessageId`/`whatsappAttempts`/`whatsappError`, mirrored on `AppNotification`) records whether that leg landed. **`users/{uid}` has no phone**, so the recipient's number is a two-hop read via `employeeId` → `employees/{id}.phone` (`whatsAppTargetForUid`); a user with no linked employee is a normal 'skipped', not an error. §9.5's six candidate templates live in `functions/src/hr/recruitment/whatsappTemplates.ts` and go **straight** through `sendWhatsApp` (a candidate is external — no uid, no in-app notification to attach to), fired from `moveCandidateStage` (1/4/5/6; ST-08 sends nothing, per the doc's own table) and `scheduleInterview` (2). Template 3, the 24-hour reminder, is a new `sendInterviewReminders` job running **hourly** — a once-daily pass would remind ~24h early or late for most slots, and taking the 24–25h window each run means every interview falls in exactly one window, so no `reminderSent` flag is needed (same stateless-by-construction argument `contractAlerts.ts` makes). New index: `interviews` outcome+scheduledAt. **The gotcha worth remembering: any function that sends must declare `secrets: [FONNTE_TOKEN]`** or `.value()` is empty and every send silently skips — that's `contractAlerts`, `moveCandidateStage`, `scheduleInterview`, `sendInterviewReminders`, the three Operations jobs, and `sendFlashReport`. `integrations/fonnte` holds §9.5's `[HR_NAME]`/`[HR_PHONE]` variables (§6.2: reuse `integrations`, no CONFIG collection) and got the `firestore.rules` block it never had — superAdmin-only read, since it holds credentials, and `INTEGRATIONS` was missing from `functions/src/lib/collections.ts` entirely.
- **New Contract Signing shipped 2026-08-18 (§9.14).** The PDF half already existed — the employee profile's "Contract Document" card (`resourceType: 'employeeContract'`) — so this only adds the routing: `submitContractForSigning` (`functions/src/hr/contracts/signing.ts`) stamps `signingStatus`/`signingApprovalRequestId`/`signedFileId` on the contract version and submits the new `'hr/contractSigning'` route, **hrManager → generalManager → director** — the first three-step chain in `routes.ts`, and distinct from the pre-existing (still unused) `'hr/contract'` renewal route rather than repurposing it. Its `registerApprovalResolvedHandler('contractSigning', …)` marks the contract signed and notifies HR; a rejection resets it to `unsigned` so HR can re-upload and resubmit instead of being stuck. Raising the request is gated by the existing `employees.update` (same precedent as Contracts/Disciplinary) — the new `contracts.sign` permission is what GM/Director hold for the signature steps, enforced by the Approval Engine's `approverRole`. **ponytail, flagged in the file:** the "digital signature" is the approval trail — approver identity, timestamp and comment in `approvalHistory` — not a cryptographically signed PDF. There is no signature-capture canvas and no signed-PDF generation (still no PDF library anywhere in this app); if a signed artifact is ever legally required that's a PDF pipeline, not a change to this flow. Approvers act through the existing approvals queue, so **no new approval UI**; the profile's Contract History card gained a "Send for signing" button (visible only once a PDF exists — the callable rejects without one) and the signing state inline.
- **§12.1 Compensation closed the gap (2026-08-17): the last unshipped `Employee` field group is now built.** `EmployeeCompensation` (`basicSalary`, `positionAllowance`/`phoneAllowance`/`transportationAllowance`, `bankAccountName`/`bankAccountNumber`) was promoted out of `employee.types.ts`'s PLANNED section — first real Firestore **sub-collection** in this codebase (`employees/{employeeId}/compensation/current`), where every other per-employee detail record (contracts, disciplinary, training) is a flat top-level collection with a denormalized `employeeId`; the sub-collection shape was already committed in the type's doc comment (field-level security — the employee doc itself is readable by GM/Director/department leaders, and `firestore.rules` can't hide individual fields). New callable `updateEmployeeCompensation.ts` full-overwrites the one `current` doc, gated by `EMPLOYEES_READ_SENSITIVE` (`employees.readSensitive` — pre-existing in `src/constants/permissions.ts` and pre-seeded on `hrManager` in `ROLE_PERMISSIONS`, but never mirrored into `functions/src/lib/permissions.ts` or consumed anywhere until now). `firestore.rules` gates the sub-collection read to `hrManager`/`superAdmin` only — narrower than the employee doc's own read rule, so GM/Director/leaders who can read the profile still can't read salary. Audit events use `resourceType: 'employeeCompensation'`, deliberately **not** `'employee'`, so they don't surface in `getEmployeeAuditLog`'s Change History card (gated only by the broader `EMPLOYEES_UPDATE`) — and deliberately no `recordEmployeeActivity` call either, since that timeline's `firestore.rules` read rule also includes GM/Director. `EmployeeProfilePage.tsx` gained a "Compensation" card gated by `PermissionGuard permission={PERMISSIONS.EMPLOYEES_READ_SENSITIVE}`, inline-edit like the page's existing Archive flow (no Dialog/Modal in this codebase). `EmployeeFormPage.tsx`/`CreateEmployeeInput` were **not** touched — compensation is set from the profile after the employee exists, same precedent as Disciplinary Records and Contract History, not folded into the single create/edit form. Manning Cost/Budget and Cost reports (previous bullet) still don't read this — out of scope for this pass.
