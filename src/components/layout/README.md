# layout

AppLayout, Sidebar, Header — structural chrome shared by every authenticated route. There is no bottom nav: on mobile the Sidebar is an off-canvas drawer opened from the Header's menu button (2026-08-19).
