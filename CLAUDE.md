# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

NourishOS is an internal operational platform for Nourish Group Indonesia, a multi-outlet F&B company — a React SPA frontend backed by **Firebase** (Auth, Firestore, Cloud Functions, Storage). It is in early milestone development. A second, public-facing app — the **Nourish Career Portal** (`portal/`, candidate intake) — ships from this same repo against the same Firebase project; see `src/features/recruitment/CLAUDE.md`. Implemented today: **HR appraisal** (including AI insights), **HR employee database**, **HR recruitment**, **HR Inventory** (uniform & asset stock), **HR Employment Contracts** (versioned history plus GM→Director signing), **HR Training** (catalog, assignment, completion), **HR Employee Engagement** (company events/activities, cost and participants), **HR Offboarding & Exit Interviews** (exit checklist, clearance statement, F009 exit interview survey), **HR Reports**, **Security** (patrol/checkpoints, Fire Extinguishers), **Operations** (Lost & Found, Incident Reports, Daily Updates, Opening/Closing Shift Reports, Projects), **Engineering** (Work Orders, Equipment Master; Preventive Maintenance is a nav stub), **Documents** (Job Descriptions, SOP Library), **Communications** (Announcements, Tasks, Team Chat, Employee Communication), **Finance** (Expense Requests), the **executive Calendar** (with Google Calendar sync), **Reports** (GM Flash Report), **Settings** (Roles & Permissions, Communication Settings), and the **dashboard** — frontend pages plus their Cloud Functions. `<ModulePlaceholder>` is in use again as of the 2026-08-25 nav restructure — one stub route remains (`/engineering/preventive-maintenance`), listed under "Current state of the tree" below. **There is no top-level Purchasing, Inventory, CRM, or Departments module** — Purchasing/Inventory/CRM were cut from scope (2026-08-15); Departments > People shipped 2026-08-16 and was deleted again the same day (see "Current state of the tree"). HR Inventory at `/hr/inventory` is the only inventory concept in the app.

PWA support (manifest, service worker) is **planned but not built** — there is no `vite-plugin-pwa` yet. Treat it as a plain Vite SPA today; don't claim or rely on offline/installable behavior until that lands.

## Environment

Development happens on Windows 11 with PowerShell as the shell. Write commands in PowerShell syntax (`;` chaining, `$env:VAR`, no heredocs into bash).

## Commands

Frontend (repo root):

Standard Vite scripts (`dev`/`build`/`preview` — see `package.json`), plus:

```
npm test         # pure unit tests (node:test) — builds functions/, then runs functions/test/*.test.mjs
npm run test:rules  # firestore.rules tests — starts the Firestore emulator (needs JAVA_HOME), runs, tears down
npm run check    # static invariant checks — mirrors, rules blocks, indexes, region. No emulator, no build.
npm run lint     # ASPIRATIONAL — eslint is not installed and has no config; this fails today
```

Cloud Functions live in `functions/`, a separate npm package with its own build and emulator commands — see [functions/CLAUDE.md](functions/CLAUDE.md).

There is **no frontend test runner** — nothing renders a component or drives a page in this repo. `npm test` exists but covers the backend's pure logic only (see below). Verification means: `npm run build` passes at the root, `npm test` and `npm run check` pass, and you exercised the affected flow in `npm run dev`.

`npm test` (added 2026-08-29) builds `functions/` and runs `node --test "functions/test/*.test.mjs"` — 152 tests, ~26s, no emulator and no JVM. The runner is Node's built-in `node:test`; **no test framework was added and none should be**. The `*.test.mjs` suffix is what `npm test` picks up; the bare `*.mjs` scripts in `functions/test/` are the emulator/seed tier and are still run by hand, one at a time. Keep the glob rather than the directory form, or those get loaded as tests. Everything in the pure tier takes its lookups as arguments, so the same code that backs the callable runs unchanged — when a module mixes pure validation with `db` access, split the pure half out the way `hr/attendance/validate.ts` does. `functions/test/README.md` has the per-file table for both tiers.

`npm run check` (`functions/test/invariants.mjs`, added 2026-08-28) is the cheap half of that. It reads the `.ts` sources as text — no emulator, no JVM, no build step — and asserts the invariants tsc structurally cannot see: that the hand-mirrored `src/constants/*` and `functions/src/lib/*` pairs agree, that every collection a query actually references has a `firestore.rules` block, that every backend-enforced permission string reaches a role in `ROLE_PERMISSIONS`, that every equality+`orderBy` query has a `firestore.indexes.json` entry (definition of done #6), and that `REGION` matches on both sides. Its parsers self-check, so a refactor that breaks them fails the run rather than silently passing. Two query call sites are not statically resolvable and it says so on every run — coverage is partial by construction, not a clean sweep.

Copy `.env.example` → `.env.local` and fill in the `VITE_FIREBASE_*` values from your Firebase project. [src/services/firebase/config.ts](src/services/firebase/config.ts) validates them at module load and throws listing exactly which are missing, so a misconfigured deployment fails loudly rather than deep inside the auth flow. Set `VITE_USE_FIREBASE_EMULATOR=true` to point the app at the local Emulator Suite instead of the live project.

**Use `npm run dev:emulator` (not `npm run dev`) when working against the Emulator Suite.** It runs `vite --mode emulator`, which loads `.env.emulator.local` on top of `.env.local` and overrides `VITE_FIREBASE_PROJECT_ID` to `demo-nourishos` — matching `firebase emulators:start --project demo-nourishos`. Under plain `npm run dev` the project id stays `nourishos-prod` from `.env.local`, and since the functions emulator serves callables under a project-scoped path, **every callable fails with `NOT_FOUND`** while Firestore reads keep working (the Firestore emulator serves any project id, just in a separate namespace). That reads like a missing function rather than a config mismatch.

## Current state of the tree (last verified 2026-08-28)

These are facts, not standards — the standards are in "Definition of done". **If your change alters any fact here, update this section in the same commit.**

Module-specific detail (what shipped when, and why it deviates from the PRDs) has moved to `<module>/CLAUDE.md` under `src/features/` — HR, Operations, Calendar, Auth, Documents, Communications, Dashboard, Finance, and Settings each have one, and it only loads when you're working in that folder. What's below is cross-cutting.

- **Navigation restructured to 11 top-level modules on 2026-08-25, and the sidebar is now expandable.** `NAV_ITEMS` in [src/components/layout/Sidebar.tsx](src/components/layout/Sidebar.tsx) gained an optional `children` array; a group auto-expands when the active route is inside it, and the collapsed rail still renders parents only. The order is Dashboard · **People** · Calendar · Operations · **Engineering** · Security · Finance · Documents · Communications · Reports · Settings. Three things to know before touching it:
  - **HR is labelled "People" and Positions / Recruitment / Employee Communication are nested under it in the nav only — their URLs did not move** (`/positions`, `/recruitment`, `/communications/employee`). All three are deliberately outside `/hr`'s `RoleRoute` gate (`positions.read` is all-authenticated; an employee must be able to open their own communication record), so **role filtering now lives on the individual nav child, not on the group**: a group renders if the parent passes its own `roles` *or* any child does, and when the parent itself is gated out its row is a non-navigating `<div>` rather than a `NavLink` — otherwise a non-HR user clicking "People" bounces off `RoleRoute`. Grouping them by URL instead would lock out exactly the users those pages exist for.
  - **Engineering (`/engineering`) is a new top-level module and Work Orders moved into it** from `/operations/work-orders` — pages are now `src/features/engineering/workOrders/`, and `/operations/work-orders/*` rewrites its tail through `WorkOrderRedirect` in `routes.tsx` so existing deep links survive. **The backend did not move**: `functions/src/operations/workOrders/` and the `workOrders.*` permission strings are untouched.
  - **One nav entry is still a `<ModulePlaceholder>` stub**: `/engineering/preventive-maintenance` (Module B — see the Equipment Master bullet below). `/engineering/assets` stopped being one on 2026-08-28. (`/security/fire-extinguishers` was a third stub until 2026-08-26 — see the Fire Extinguishers bullet below.) This is what put `ModulePlaceholder` back in use after `/reports` stopped being a stub. No collections, rules, callables or permission strings exist yet for the one remaining stub — which also means it does not appear on Settings > Roles & Permissions, since that page derives its module list from `PERMISSIONS`. Its collection name is already reserved and unused in `src/constants/collections.ts` (`preventiveMaintenance`); `equipmentInspections` is likewise reserved and still unused — Module A has no inspection records, see below. Documents > "Company Forms" is relabelled **"Company Documents"** — label only, the `companyForms` route/collection/service are unchanged. Team Chat stays out of the nav (reached from the header `ChatBell`).
- **Documents reach the app as ISO strings, not `Timestamp`s.** `queryDocuments`/`subscribeToCollection` take genuine `QueryConstraint`s, so `where`/`orderBy`/`limit` are pushed down to the server instead of filtering a whole fetched collection client-side. [src/services/firestore/normalize.ts](src/services/firestore/normalize.ts) converts `Timestamp` → ISO string at the single read boundary (recursively, for timestamps nested in maps/arrays), which is why `BaseDocument.createdAt` and friends are still typed `string` rather than reverting to `Timestamp` — components, format helpers, and `src/utils/date.ts` never see a `firebase/firestore` type. Cloud Functions still write real `serverTimestamp()`s.
- **Vite warns that the main JS chunk is >500 kB** — a warning, not a failure. Code-splitting is future work; don't treat the warning as a regression you introduced.
- **Design tokens migrated to v3 ("Basalt") on 2026-08-11.** `src/styles/globals.css` and `tailwind.config.ts` now carry the Basalt palette (Pandan `#0E4F47` primary, cool paper `#F4F5F3` bg, status colors = workflow ramp: success `#15803D`/warning `#B45309`/error `#B91C1C`/info `#1D4ED8`, radii unchanged 4/8/12/12, shadows re-tinted neutral cool instead of warm), fonts Archivo Variable + IBM Plex Mono via Google Fonts `<link>` in `index.html` (self-hosting via `@fontsource` deferred — no service worker exists yet to justify it, see `ponytail:` comment there), `Button`'s secondary variant is now bordered/sunken neutral (dropped filled Deep Olive — two filled brand-ish buttons per screen violated Basalt's rationing rule), default `Button` height bumped 44px→48px (universal touch minimum). **`StatusPill` is built** (`src/components/ui/status-pill.tsx`) — generic, takes `{tone, icon, label}`, not coupled to any one status enum, so each module owns its own status→tone/icon mapping. **Not yet migrated**: the field/desk density switch (`data-density` attribute, viewport-auto per D1) doesn't exist in code — components are still single-density; `ApprovalCard`/`AuditTimeline`/`TaskRow`/`MetricTile`/`OutletBadge`/`QueueBanner` (STYLE_GUIDE.md § Shared components) are still unbuilt; the offline queue UX (D3) has no implementation. Dark mode (`.dark` class in globals.css) has Night pass hex values wired but no role-based-default trigger (D2) implemented — it's manual-toggle-only today. See `docs/core/STYLE_GUIDE.md` v3 and `docs/2026-08-11-nourishos-design-system.md` for full spec and signed-off decisions.
- **`src/components/ui/` primitives are native-HTML, never Radix** — `Select`/`Input`/`Checkbox`/`Switch`/`Tabs`/`Tooltip` etc. style native elements rather than pulling in a headless-component dependency. Match that convention when adding one.
- **There are no `/demo/*` preview routes.** Two constants the old demo data owned live in the shipped format modules: `RETENTION_DAYS` in `lostFoundFormat.ts`, `INCIDENT_ROUTING` in `incidentFormat.ts`.
- **The HR_OPERATIONS.md gap-closing pass of 2026-08-18 shipped six things** (module detail in the per-module `CLAUDE.md` files): the **Fonnte WhatsApp channel** behind the existing Notification Engine (§9.5/§9.11/§13.1), **Google Calendar sync** (§9.3/§14.4), **Project Management** at `/operations/projects` (§9.8 — the last PRD module with no code), **Contract Signing** HR→GM→Director (§9.14), the **GM Flash Report** at `/reports` (§9.12-F09/E12), and **role-scoped dashboard widget sets** (§9.9/§9.13). Two new secrets have to be provisioned or those first two features are silent no-ops: `FONNTE_TOKEN` and `GOOGLE_CALENDAR_SA_KEY` (`firebase functions:secrets:set`), plus `integrations/fonnte` and `integrations/googleCalendar` config docs. **§6.4/§9.6's weighted-component appraisal scoring was closed on 2026-08-24 — not by either Option A or B, but by a third path (Appraisal v2, see the next bullet) that supersedes both.**
- **Positions Master and Appraisal v2 shipped 2026-08-24** (`docs/core/POSITIONS_MASTER_DESIGN.md`, `docs/modules/appraisal-v2-design.md`; module detail in `src/features/hr/CLAUDE.md`). A new `positions` collection is the org-wide JD catalogue (per-position tier, key responsibilities, `appraisalScorerPositionId`), seeded thin — tier ladder + the design doc's §5 Department Head mapping only, every position `positionStatus: 'draft'` with empty `keyResponsibilities` since no source Drive `.docx` library exists in this repo; HR fills real content in-app via the new `/positions` module and its own HR Manager → Department Head → GM approval chain. **Deliberate, documented deviation from the design doc's own acceptance criteria: `src/constants/positions.ts`'s `POSITION_CATALOG` (already a 66-entry typed catalog as of the 2026-08-17 pass, not the "5-item starter set" the design doc's problem statement assumed) is kept exactly as-is** rather than removed — the new `positions` collection is seeded 1:1 on the same `PositionId` slugs on top of it, since the catalog's department/outlet-scoped dropdown logic has nothing to do with JD content or appraisal and rewriting the ~13 files that read it synchronously would have been pure regression risk for zero behavioral gain. `migrateEmployeePositions` (Super Admin, `positions.seed`) populates the new `employee.positionId` from the already-validated `employee.position` — a straight copy plus a validation pass, not fuzzy text matching, since both fields share the same `PositionId` slug space; **it has not yet been run against any real environment**, only built and typechecked, so `employee.positionId` is unpopulated until someone runs it. Appraisal v2 replaces the shipped 1-5 single-reviewer instrument with per-position criteria generated (via `generateAppraisalTemplate`/`approveAppraisalTemplate`) from each position's Key Responsibilities, dual-scorer (Dept Head 60% + HR 40%, 1-10 scale, /100 final) for tier IV-VIII, solo GM-scored for I-III, five rating bands, and a confidential low-score recommendation subcollection HR/GM/Director can never read on their own appraisal. The old v1 module's callables (`submitAppraisal`, `seedAppraisalTemplates`, `triggerProbationReviews`) are deleted, not deprecated — historical v1 `appraisals`/`appraisalTemplates` docs stay readable and render distinguishably (branch on `scoringModelVersion`), never rescaled. **Known, named launch-time gap, same class as Employee Communication's identical one**: nothing in this codebase populates `users/{uid}.employeeId` yet, so §2.7's "authenticated self-acknowledgement" path for senior (soloScorer) staff never fires — every acknowledgement is on-device (`SignaturePad`, new, native canvas, no dependency) until account linking exists as its own follow-up. **This session's build was verified by `npm run build` and `npm --prefix functions run build` only** — no Java/JRE was available in this environment to run `firebase emulators:start`, so the flow was not exercised end-to-end against the emulator; do that before treating either module as production-ready.
- **Milestone announcements shipped 2026-08-25** (module detail in `src/features/communications/CLAUDE.md`): a `milestoneAnnouncements` scheduled job (07:00 WITA) auto-creates **and auto-publishes** a company-wide announcement for four employee milestones — birthday, work anniversary, new hire, farewell — sourced from `Employee.birthDate`/`joinDate`/`lastWorkingDate`, plus a personal WhatsApp to the celebrant. These are the first announcements no human writes, and the first with a reader-side interaction beyond the implicit read receipt: a new `announcementWishes` collection (one document per person, emoji + optional message, `sendMilestoneWish`) with a client-side tally and a denormalised `wishCount` on the announcement. Two things follow from the auto-publish: dedup is a **deterministic announcement id** (`milestone_{kind}_{employeeId}_{date}` via `.create()`), not the stateless exact-day match `contractAlerts.ts` relies on, and `publishInternal` is now exported from `announcements.ts` and called with a `SYSTEM_USER` actor. No new permission string (`sendMilestoneWish` is identity-gated) and no new composite index. Confetti is CSS keyframes in `globals.css` — **still no animation dependency in this repo**. Test: `node functions/test/milestone-match.mjs` (no emulator).
- **Fire Extinguishers (APAR) shipped 2026-08-26** (`docs/modules/fire-extinguisher.md` v1.1; module detail in `src/features/security/CLAUDE.md`), turning the third nav stub into a real module at **`/security/fire-extinguishers`** — not the doc's `/operations/apar`, since the stub, Security's ownership of the inspection and the un-gated `/security` subtree all pointed the same way. Two new collections (`fireExtinguishers`, `fireExtinguisherInspections`), four callables plus two scheduled jobs (`generateMonthlyAparRounds` 1st 06:00 WITA, `checkAparExpiry` daily 06:30 WITA) in `functions/src/security/fireExtinguishers/`, and two new permission strings — **`apar.manage` and `apar.inspect`, not the doc's five**: reads are rules-gated, so `apar.view`/`apar.viewAllOutlets` would have checked nothing. A monthly round is one Task per outlet keyed by `referenceId: '${outletId}__${periodMonth}'` (dedup, period source, and round identity in one string) holding no unit list, so the register is read live; inspection uniqueness is the deterministic doc id `${extinguisherId}__${periodMonth}` + `.create()`. **Shipped online-only against the doc's own §10 ship-block on D3** — per-unit submission means a dropped connection costs the unit in hand, and AC-3 stays knowingly unmet. Usage/discharge reporting, the compliance dashboard and `checkOverdueAparRounds` are deferred (doc §15); a discharge is filed as an Incident Report plus a Work Order today. Two new composite indexes. Test: `node functions/test/apar-inspection.mjs` (no emulator).
- **HR Reports Redesign shipped 2026-08-25** (module detail in `src/features/hr/CLAUDE.md`): Turnover rewritten (outlet×department grouping, MTD/YTD, active/resigned totals); Manning Budget & Cost consolidated onto one page (budget vs actual by a hardcoded season calendar, manning cost vs monthly revenue) — **deliberately reverses a prior confirmed decision**: Manning Cost is now sourced from a new `payrollRecords` collection rather than `hrStockMovements`, and the two old procurement-spend-flavored reports (`ManningCostReportPage.tsx`, `BudgetAndCostReportPage.tsx`) are deleted, generalized into the new Inventory Cost report instead. New Payroll module (`/hr/payroll`, bulk CSV import, full Indonesian deductions, server-computed net pay) and a manual Monthly Revenue entry concept — both genuinely new, no prior payroll or revenue data existed anywhere in this app. Training gained a `durationHours` field feeding a new Training Hours report. No new charting dependency (a plain-CSS `BarDiagram` shared component instead) and no new Firestore composite indexes.
- **`google-auth-library` is now a direct dependency of `functions/`** (it was already installed as a firebase-admin transitive). It mints the service-account JWT for Google Calendar; the API calls themselves are plain `fetch`. **`googleapis` is deliberately not installed** — it's enormous next to the four endpoints in use.
- **`docs/core/HR_OPERATIONS.md` is the HR & Ops PRD (v2.0.0)** — code cites its §9.x/§12.x numbering.
- **The backend runs on WITA (`Asia/Makassar`) — see [functions/CLAUDE.md](functions/CLAUDE.md)**, which has the full rules and the helper functions (`functions/src/lib/timestamps.ts`).
- **superAdmin now bypasses every permission-string check, not just the callables that call `requireSuperAdmin` directly (2026-08-15).** `requirePermission`/`requireAnyPermission` (`functions/src/lib/rbac.ts`) return early when `user.roleId === 'superAdmin'`, and `AuthProvider.tsx` mirrors it client-side (`setPermissions(Object.values(PERMISSIONS))` instead of reading `roles/superAdmin`'s Firestore doc when the signed-in user is superAdmin). This closes the exact gap `requireSuperAdmin`'s own doc comment already named: superAdmin is deliberately absent from `ROLE_PERMISSIONS` and its `roles/superAdmin` doc was seeded by hand once, so every permission string added by every module since needed a manual Firestore edit to actually reach it, and evidently never fully did. Read access needed no change — `firestore.rules`' `isElevated()`/`hasAnyRole([...])` blocks already include `superAdmin` consistently across every collection (spot-checked the whole file). The Settings > Roles & Permissions page still reads/writes `roles/superAdmin`'s real permissions array for visibility/audit purposes; it's just no longer what enforcement depends on.
- **The Candidate Portal shipped 2026-08-19** (`docs/modules/candidate_portal.md`, `docs/modules/employment-application-form.md`; detail in `src/features/recruitment/CLAUDE.md`). A **second Vite app** lives at `portal/` — its own entry, config (`vite.portal.config.ts`), bundle (`dist-portal/`) and hosting target, but **not** its own npm package: it shares the root `node_modules`, `tailwind.config.ts` and `src/styles/globals.css`, and imports nothing else from `src/`. Scripts: `npm run dev:portal` / `dev:portal:emulator` (port 5174) and `build:portal`. Candidates get **no Firebase Auth account** — a hashed magic-link token on the candidate document is the whole credential (`functions/src/recruitment/portal/token.ts`), and every portal callable is **unauthenticated**, so `resolveCandidateByToken` is the trust boundary the way `requireActiveUser` is everywhere else. New collection `discResults` (server-scored, write-denied, hrManager/GM/director/superAdmin read); new sub-collection `candidates/{id}/confidential/application` for F010's health/criminal/salary answers behind the new **`recruitment.viewSensitive`** permission; new env var `PORTAL_BASE_URL` on the functions side (a plain env var, not a secret — it is a public URL). **Known gap: no App Check** — the only abuse control is a 30-day same-phone/same-vacancy duplicate check, so turn App Check on before the portal is announced publicly. Smoke test: `node functions/test/portal-flow.mjs` against the emulator.
- **Employee Communication shipped 2026-08-19** (`docs/modules/employee_communication.md`; detail in `src/features/communications/CLAUDE.md`), closing a sub-module decision deferred since 2026-08-15. It **extends the existing `disciplinaryActions` collection** instead of adding the spec's own `communicationRecords` (§19), so `src/features/hr/disciplinary/` and the `/hr/employees/:employeeId/disciplinary/*` routes are **gone** — the register and every page now live at `/communications/employee`, since the employee is one of the audiences and `/hr/*` is HR/GM/Director-only. `disciplinaryActions` is consequently **no longer HR/elevated-read-only**: it gained a department-head branch (keyed on a now-denormalized `departmentId`) and an own-record branch for the employee (`employeeUid == request.auth.uid && releasedToEmployee == true`, resolved server-side the same way `audienceUids` is). Second conditional approval route in `shared/approval/routes.ts` (`'hr/employeeCommunication'`, Dept Head → HR → GM), one new scheduled job (`expireCommunicationRecords`, 08:00 WITA), and four new callables. **One new permission string**, `employees.communicate` (→ `LEADER`): `employees.update` is granted to `hrManager` alone, so reusing it would have left §5.4's department-head creation impossible. The callables accept either string, so hrManager/superAdmin need no `roles/{roleId}` edit — hand-adding it to the leader docs is what turns department-head creation on later. §32's PDF is a Tailwind `print:` stylesheet, not a PDF pipeline: **there is still no PDF library in this repo**. Test: `node functions/test/communication-steps.mjs` (no emulator needed).
- **Global Search**: the header `SearchBar` was a disabled placeholder (2026-08-15) — it now navigates to `/search?q=…` on Enter. [src/services/shared/searchService.ts](src/services/shared/searchService.ts) fires five independent Firestore prefix-match range queries (Employees by `fullName`, SOPs by `topic`, Job Descriptions by `title`, Announcements by `title`, Tasks by `title`), each wrapped so a rules-denied collection contributes zero results rather than failing the whole search — no new indexes (a same-field range+orderBy is auto-indexed). Work Orders isn't in the sweep yet; "Reports" are aggregation pages, not records, so excluded.
- **Docs mark unshipped functions/collections/components as "Planned"** and describe the Firebase design, which is what runs. One stale detail: they place Firebase config under `src/`, and it's at the repo root now.
- **`Departments > People` shipped at `/departments/people` on 2026-08-16 and was deleted the same day** — a second nav entry point onto HR data (Active Employee, Performance Review, Employee Communications sub-pages) was built, then scrapped in favor of surfacing a department-scoped active-employee count directly on each department manager's dashboard instead (see `src/features/dashboard/CLAUDE.md`). `src/features/departments/` no longer exists; `/hr/*` was never touched by it and needs no cleanup. One artifact from that module survives because it's independently useful: scheduled Cloud Function `triggerProbationReviews` (`functions/src/hr/appraisal/probationReviewTrigger.ts`, 06:00 WITA, same exact-day-match/stateless shape as `contractAlerts.ts`) auto-creates a draft `'probation'` `Appraisal` 100 days after `Employee.joinDate`, via `createAppraisalInternal` (split out of `createAppraisal.ts` the same way `createEmployeeInternal` was extracted). **Known gap, half-closed (2026-08-17):** `Employee.position` is now a validated `PositionId` — a department-scoped `<Select>` on `EmployeeFormPage.tsx` (see `src/features/hr/CLAUDE.md`) replaced the old free-text `<Input>`, and `createEmployee`/`updateEmployee` reject anything outside `functions/src/lib/positions.ts`'s catalog. The trigger still falls back to notifying `hrManager` rather than throwing for two remaining cases: **only 5 catalog positions have a seeded probation `AppraisalTemplate`** (unchanged — a content-authoring gap, not a validation one), and **legacy employees hired before this change** still carry a free-text value that won't match any `positionId`. Also note: no create-UI for quarterly/annual appraisals exists anywhere in the app.
- **Equipment Master (Module A) shipped 2026-08-28** (`docs/modules/equipment-master-design.md` v1.0), replacing the `/engineering/assets` `<ModulePlaceholder>` stub — the asset registry Module B (Preventive Maintenance, not built) will schedule against. New `equipment` collection (`src/constants/collections.ts`'s reservation now in use; `equipmentInspections` stays reserved and unused — Module A has no inspection records, that's Module B's), seven callables plus one approval-resolved handler in `functions/src/operations/equipment/`, four pages under `src/features/engineering/equipment/`. Two deliberate deviations from the design doc, both because the doc assumed infrastructure that doesn't exist in this codebase:
  - **§3.1's `outlets.areas` is a hardcoded constant, not a Firestore field** — there is no `outlets` collection (outlets are the same hand-maintained `OUTLETS`/`OUTLET_DEPARTMENTS` pair `src/constants/organization.ts` and `functions/src/lib/organization.ts` have always been). `OUTLET_AREAS` and a new `OUTLET_CODES` (three-letter codes feeding §3.5's `{OUTLET}-{CAT}-{NNN}` asset code format) were added to both mirrored files alongside them. `OUTLET_AREAS`'s actual per-outlet lists are a first pass, not a signed-off register — same content-not-engineering caveat the design doc's own A-O3 names.
  - **§5.2's "the asset's own Outlet Manager approves" needed a real Approval Engine capability that didn't exist**: `approveStep.ts` authorized purely by role id, so any `outletManager` anywhere could have cleared any outlet's decommission step. `ApprovalStepDefinition` (`functions/src/shared/approval/types.ts`) gained an optional `approverOutletId`, checked in `approveStep.ts` alongside the existing role check (skipped for `OVERRIDE_ROLES`) — additive to the shared engine, every other route's steps omit the field and are unaffected. `notifyUsersByRole`/`notifyStepApprovers` gained a matching optional `outletId` filter so the submit/advance notification only reaches that one outlet's managers. New route `'operations/equipmentDecommission'` (`shared/approval/routes.ts`), single step, `buildEquipmentDecommissionApprovalSteps` in `functions/src/operations/equipment/approvalSteps.ts`.
  - **No `equipment.view`/`view_all` permission strings**, unlike the design doc's §6.1 — reads are outlet-scoped in `firestore.rules` instead (elevated roles `superAdmin`/`director`/`generalManager`/`engineering` see every outlet, everyone else only `resource.data.outletId == request.auth.token.outletId`), matching the one existing precedent module (Fire Extinguishers) having no enforcement point for view permissions either. Only `equipment.manage`/`equipment.import`/`equipment.decommission` are real permission strings, granted to `engineering` in `ROLE_PERMISSIONS` — **existing `roles/engineering` documents need these added by hand** or via `node functions/tools/sync-role-permissions.mjs --prefix equipment. --apply`, the same standing gotcha every prior module has hit.
  - Two-phase CSV import (`previewEquipmentImport`/`commitEquipmentImport`) follows Payroll's own convention: the client re-sends the same parsed rows to commit as it sent to preview (no server-side preview cache), and the `previewToken` is a lightweight base64 timestamp whose only job is a 15-minute soft-expiry nudge, not a lookup key — `commitEquipmentImport` re-validates every row from scratch either way. Asset codes are allocated in bulk (`allocateEquipmentAssetCodes`, one transaction per distinct outlet+category key present in the file, not one per row) so a 500-row import stays fast. The list page filters area/category/criticality/status client-side, the same "dozens, not thousands" assumption `fireExtinguisherService.ts` already makes — but **outlet is a query constraint, not a client-side filter** (`equipment` on `outletId`+`assetCode` in `firestore.indexes.json`). This bullet used to say the page read "the whole (rules-filtered) collection": rules do not filter, and that misreading shipped a register that was denied outright for every non-elevated user until 2026-08-29. `EQUIPMENT_ALL_OUTLET_ROLES` in `src/constants/equipment.ts` mirrors the rules block's elevated set and is deliberately not `CROSS_OUTLET_ROLES`, which omits `engineering`.
  - Decommission approve/reject is a page-level control on `EquipmentDetailPage.tsx` itself, not the Pending Approvals widget — see the 2026-08-31 bullet below, which corrects this and three other pages that shipped with the same wrong assumption.
  - The design doc's own "hard blocker" A-O1 (conflicting `firestore.rules` files in two locations) does not describe this tree — there is only one `firestore.rules`, at the repo root; the historical `src/` copy was removed in an earlier migration, well before this pass.
  - **Not exercised against the emulator this session** — `npm run build` and `npm --prefix functions run build` are both green; no Java/JRE was available to run `firebase emulators:start`. Verify create → status change → outlet transfer → decommission-submit → approve-as-correct-outlet-manager (should succeed) → approve-as-different-outlet-manager (should now be rejected — this is new engine behavior) → import preview/commit before treating this module as production-ready.
- **The RBAC role set became the org role ladder on 2026-08-29** (module detail in `src/features/auth/CLAUDE.md`): `DEPARTMENT_ROLES` now offers one role per HR job title — 44 roles, up from 18 — on both the `/register` form and Settings > Roles & Permissions. Thirteen titles reuse an existing role id and changed label only (`marketing` is Creative & Marketing Manager, `hrManager` is Jr. HR Manager, `security` is Security Supervisor, and so on), so `firestore.rules`' hardcoded role lists and every existing `users/{uid}` document keep working. Role selection is now **outlet-scoped**: a new `rolesFor(outletId, departmentId)` intersects `DEPARTMENT_ROLES` with a new `OUTLET_ONLY_ROLES`, so `kitchen` resolves to the baking ladder at `the_bakery_kitchen` and the line-cook ladder at the three restaurant outlets, and `wholefood_retail` offers Manager/Supervisor only at `wholefood_ungasan` — `registerUser` validates through the same function. Two new permission tiers (`SUPERVISOR`, `RANK_FILE`) sit beside `LEADER` in `functions/src/lib/organization.ts`; **no new permission strings and no shipped role's set changed, so no `roles/{roleId}` document needs a hand edit** — the usual standing gotcha does not apply here. One role, `restaurantMaintenanceManager`, was added to five `firestore.rules` role lists (`isLeader()`, the department-scoped `employees`/`disciplinaryActions`/`appraisals` branches, `incidentReports`, `workOrders`). Test: `node --test functions/test/role-ladder.test.mjs` (no emulator). **`npm run test:rules` was not run** — no Java/JRE in this environment; the rules edits are purely additive to existing role lists and `rules.mjs` references none of the changed names. **The "keep the id, change only the label" choice for four of those titles was reversed later the same day — see the next bullet.**
- **Four department-leader role ids were renamed to match their labels, and `outletManager`/`staff` were removed outright, later on 2026-08-29** — superseding the "reuse an existing role id" choice the bullet above made for `floorLeader`/`barLeader`/`kitchenLeader`/`bakeryLeader` specifically. `floorLeader`→`restaurantSupervisor`, `barLeader`→`barManager`, `kitchenLeader`→`headChef`, `bakeryLeader`→`chiefBaker` (both key and value renamed in `ROLES`, `DEPARTMENT_ROLES`, `OUTLET_ONLY_ROLES`, `ROLE_LABELS`, `ROLE_PERMISSIONS`, `INTERVIEWER_ROLES`, and every `firestore.rules` role list — everywhere this session's own read found a live reference). `outletManager` and `staff` are gone from `ROLES`/`ROLE_LABELS`/`ROLE_PERMISSIONS`/`DEPARTMENT_ROLES` entirely, not just unoffered: `housekeeping` (staff's only department) is left with an empty role list, since nobody currently holds it and a migration script shouldn't invent a replacement role for a live department. `outletManager` was actively depended on as "the one role every outlet has" in four places with no outlet restriction of its own — equipment decommission approval (`buildEquipmentDecommissionApprovalSteps`), `INCIDENT_ROUTING`, `ESCALATION_THRESHOLDS` level 1, and the Lost & Found/Fire-Extinguisher-expiry notify calls — so a new `OUTLET_LEAD_ROLE` map (`functions/src/lib/organization.ts`, server-only, no client mirror) resolves "this outlet's own lead" per outlet (`restaurantManager` for the three standard outlets, `chiefBaker`/`wholefoodLeader`/`wholefoodSupervisor`/`barManager`/`generalManager` for the rest) and is used wherever the outlet actually varies; the other three call sites, which were already unscoped by outlet before this change, got a plain `restaurantManager` substitution instead — no new behavior, since `restaurantManager` already carried `outletManager`'s exact permission set. `functions/tools/migrate-role-ids.mjs` is the one-time live-data fixup (renames `users/{uid}.roleId` + custom claims + `roles/{roleId}` docs for the four renames; only *reports* any user still on `outletManager`/`staff`, never reassigns one) — **not yet run against any real environment**. `npm run build`, `npm --prefix functions run build`, `npm test` (161/161) and `npm run check` (20/20) are all green; `npm run test:rules` was not run (no Java/JRE in this environment) even though this pass edited role lists in every branch that suite covers — run it before treating `firestore.rules` as verified.
- **Attendance shipped 2026-08-28** (`docs/modules/attendance.md`; module detail in `src/features/hr/CLAUDE.md`), a new `/hr/attendance` module and an 11th HR Report. A monthly aggregate ledger, deliberately not a time-and-attendance system — no punch times, no daily grid, no leave request workflow (§1.2). Two new collections, `attendancePeriods` (company-wide, one per calendar month) and `attendanceRecords` (one per employee per period), modelled closely on Payroll's own batch/payslip shape: CSV upload, an advisory preview callable (`previewAttendanceImport`), then a commit callable (`importAttendancePeriod`) that re-validates server-side and writes everything in one transaction, then `submitAttendancePeriod` onto a new fixed two-step `'people/attendancePeriod'` approval route (HR Manager, then GM). Records carry an `isApproved` boolean, stamped true only when that approval resolves — the same `isIssued` gate `Payslip` uses, and for the same reason: an outlet-scoped leader must not see a figure that might still be rejected or corrected. A correction (§6.2) is a whole new period document, never an edit: the original flips to `closed`/`supersededByPeriodId`, the correction carries `supersedesPeriodId`, and a month's first-ever import is the one case where the document id equals the `period` string ('YYYY-MM'); a correction gets an auto-generated id since that slot is already taken, so every query/sort goes through the stored `period` field, never the doc id. The nine-code taxonomy (§2) and legacy alias folding (`NPL` to `UL`, `DPH`/`DPN` to `DP`, `OFF` to `DO`, §2.2 D4) live in `functions/src/hr/attendance/codes.ts`, pure and mirrored client-side in `src/constants/attendance.ts`, pinned by `node functions/test/attendance-import.mjs` (no emulator). Reuses `ValidationIssueList` from Payroll's own import components rather than duplicating it (identical issue shape). New permissions `attendance.import`/`attendance.approve`/`attendance.viewAllOutlets`/`attendance.viewOwnOutlet`/`attendance.export` (the doc's reserved `attendance.readOwn` is deliberately not scaffolded, same call as `payroll.readOwn`), same standing gotcha as every prior module: existing `roles/{roleId}` docs need these added by hand or via `sync-role-permissions.mjs`. A new monthly `onSchedule` job (`remindAttendanceImport`, 1st 06:00 WITA) creates a Task Engine reminder for HR Manager. The source CSV is archived to Storage via the callable itself (Admin SDK `bucket().save()` plus `createFileMetadataInternal`, same pattern `uploadCandidateDocument.ts` uses) rather than a client-side Storage upload first — Payroll's own batches never archive their source file at all, so this is new plumbing, not a reused path. No new Firestore composite indexes (every query is single-field or a two-equality-filter combination). This session's build was verified by `npm run build` and `npm --prefix functions run build` only; not exercised against `firebase emulators:start`.
- **Approve/Reject was missing from the UI everywhere, and was fixed on 2026-08-31.** Every approval-routed detail page (`RequisitionDetailPage.tsx`, `ExpenseDetailPage.tsx`, `EquipmentDetailPage.tsx`, `CommunicationRecordDetailPage.tsx`) carried a comment saying approve/reject belonged on the dashboard's Pending Approvals widget instead — but `PendingApprovalsWidget.tsx` was, and still is, read-only by design and says the opposite: that the record's own page owns the decision. Neither side actually built it, so `approveStep`/`rejectStep` ([functions/src/shared/approval/](functions/src/shared/approval/), wired client-side in [src/services/shared/approvalService.ts](src/services/shared/approvalService.ts)) had no caller anywhere in the app — nobody could approve anything, in any module, ever, through the UI. Fixed by adding an "Approval" card to all four pages: a live `subscribeToApprovalRequest` (new) drives a new pure `approvalService.canActOnApprovalRequest(request, actor)` — mirroring `approveStep.ts`'s own role/outlet/self-approval checks client-side, UX only, the callable still re-verifies everything — which gates an Approve/Reject block (reject requires a comment; approve's is optional) rendered above each page's existing approval-history list. `ApprovalStepDefinition` (`src/types/workflow.types.ts`) gained the `approverOutletId?: string` field it was already missing relative to its backend mirror, needed for Equipment's outlet-scoped decommission step to gate correctly. `RequisitionDetailPage.tsx` had no approval-history section at all before this and gained one alongside the actions. No backend change — the callables and their authorization already worked correctly; this was purely a missing frontend call site.

- **`hrGeneralAdmin` (POSITIONS.md §3 Level VI, added in the 2026-08-29 role ladder pass) was shut out of the whole `/hr` tree, fixed 2026-08-31.** Granting it `employees.*` permissions through Settings > Roles & Permissions had no visible effect because module access for `/hr` runs through three separate hardcoded role allow-lists, none of which the ladder pass touched: `HR_REPORT_ROLES` (`src/constants/roles.ts`, doubles as the `RoleRoute` gate via `routes.tsx`'s `HR_MODULE_ROLES`), a second hand-copied list that had drifted inside `Sidebar.tsx` (now just imports `HR_REPORT_ROLES` instead of maintaining its own), and `firestore.rules`' `employees`/`employees/{id}/compensation`/`disciplinaryActions` read rules, which are role-hardcoded the same way every other elevated-read branch in that file is. Permission strings (`employees.read`/`update`/etc.) were never the blocker — `requirePermission` already reads the live `roles/{roleId}` doc, so the checkboxes worked exactly as configured at the callable layer the whole time; the gates that failed were the coarser role-membership ones RBAC.md documents as a separate layer from permission strings. Added `hrGeneralAdmin` to all four. **`npm run test:rules` was not run** — no Java/JRE in this environment; the rules edits are additive to existing role lists in branches that suite already covers.
- **`hrGeneralAdmin` still couldn't archive employees, run the offboarding checklist, or conduct exit interviews after the fix above — a second, narrower instance of the same gap, closed 2026-08-31.** The `/hr` tree was open, but three permission-string and rules gates specific to this workflow still didn't name the role: `functions/src/lib/organization.ts`'s `ROLE_PERMISSIONS.hrGeneralAdmin` had no `employees.*`/`exitInterviews.view` entries at all (unlike the live Firestore doc, which the earlier Settings-UI pass had already granted `employees.*` on — this fixes the map as the source of truth for any future re-claim or `sync-role-permissions.mjs` run, it doesn't retroactively change anything already live), `firestore.rules`' `isHrOrAbove()` helper (gates `recruitments`/`candidates`/`interviews`/`onboardingChecklists`/`offboardingChecklists`/`hrStockMovements`/`trainingAssignments` — one edit fixes all seven), and the `exitInterviews` collection's own tighter, deliberately-narrower read rule (`exit-interview.md` §4 keeps this one hrManager/superAdmin-only, excluding even GM/Director — `hrGeneralAdmin` joins that same short list rather than going through `isHrOrAbove()`). Junior HR Manager (`hrManager`, relabeled 2026-08-29) already had `exitInterviews.view` and every other gate here — no change needed for that role. **Still needs, before this is live**: `firebase deploy --only firestore:rules`, and the live `roles/hrGeneralAdmin` Firestore doc updated — either by hand through Settings > Roles & Permissions, or `node functions/tools/sync-role-permissions.mjs --prefix employees. --apply` plus `--prefix exitInterviews. --apply` (add-only, won't touch the permissions already granted through the UI).
- **`archiveEmployee` had no undo — added `unarchiveEmployee` 2026-08-31.** Reported as "enable to unarchive employee" after the above. New callable (`functions/src/hr/employees/archiveEmployee.ts`), gated by `employees.delete` like `archiveEmployee` itself (whoever can archive can undo it): flips `status` back to `active`, clears `resignationDate`/`resignationReason`/`lastWorkingDate` (a later archive overwrites them with fresh values anyway, so nothing depends on keeping the stale ones around), and deliberately leaves the offboarding checklist and any exit interview on file untouched — they're history of what happened, not something reinstating someone should erase, the same reasoning `archiveEmployee`'s own header already states. `EmployeeProfilePage.tsx`'s Separation card gained an "Unarchive employee…" control next to the existing "View offboarding checklist" link. New `EmployeeActivityType` value `reactivated`, mirrored in both `src/constants/hr.ts` and `functions/src/hr/employees/helpers.ts` — the compiler caught one more place needing it, an exhaustive `Record<EmployeeActivityType, string>` label map on `EmployeeActivityReportPage.tsx`, which is the only such map in the frontend (activity timelines elsewhere render `activity.description` directly, not a type-keyed label). `npm run build`, `npm --prefix functions run build`, and `npm run check` (27/27) all green; not exercised against the emulator.
- **`training.assign` reactivated 2026-08-31 — a dormant permission string, not a new one.** It was defined for the old v1 flat training module, marked "superseded... no callable checks it any more, kept defined so live roles/{roleId} documents that hold it still resolve" once the v2 catalogue (`trainingTopics`/`trainingBindings`) shipped, and stayed dead ever since. `generateTrainingAssignments`'s manual/backfill callable (`functions/src/hr/training/generateTrainingAssignments.ts`) now accepts `requireAnyPermission([TRAINING_MANAGE, TRAINING_ASSIGN])` instead of `TRAINING_MANAGE` alone — the narrower companion, letting a department head issue their own team's sequence without the catalogue-curation power (`seedTrainingCatalog`/`overrideTrainingGate`) that stays `TRAINING_MANAGE`-only. Granted in `functions/src/lib/organization.ts`'s `ROLE_PERMISSIONS` to `director`, `generalManager`, `hrGeneralAdmin`, `headChef`, `chiefBaker`, `wholefoodLeader`, `barManager`, `restaurantManager`, `finance`, `purchasing`, and `sousChef` (`superAdmin` bypasses; `hrManager` already had the broader `training.manage`) — this exact role list was requested by name. `EmployeeProfilePage.tsx`'s Training card, previously gated to `employees.update` alone (so only hrManager/superAdmin could even see it) and rendering no action at all, now gates on `anyOf: [EMPLOYEES_UPDATE, TRAINING_MANAGE, TRAINING_ASSIGN]` and has an "Assign Training" button calling the callable. `firestore.rules`' `trainingAssignments` read rule gained `finance`/`purchasing`/`sousChef` (department-scoped, inline — deliberately not folded into the shared `isLeader()`/`isDepartmentHead()` helpers, which would have swept in `security`/`engineering`/`marketing` too, roles nobody asked to see this collection); `isLeader()` itself already covered `headChef`/`barManager`/`chiefBaker`/`wholefoodLeader`/`restaurantManager`, and `isHrOrAbove()` already covered `director`/`generalManager`/`hrGeneralAdmin`/`hrManager`/`superAdmin`. **Still needs, before this is live**: `firebase deploy --only firestore:rules`, and the eleven newly-granted roles' live `roles/{roleId}` docs updated via Settings > Roles & Permissions or `node functions/tools/sync-role-permissions.mjs --prefix training. --apply`.
- **Calendar events gained an optional `otherParticipants` free-text field 2026-08-31**, for guests with no NourishOS account (`src/types/calendar.types.ts`, `functions/src/shared/calendar/events.ts`, `src/features/calendar/calendarService.ts`). Deliberately kept separate from the `participants` uid array rather than merged into it — that array drives conflict detection, in-app notifications, and Google Calendar sync, all of which resolve each entry as a real user, so a free-text name in the same array would either crash or silently no-op each of those. `otherParticipants` is pure informational text: stored, never touched by any of the three. `CalendarEventFormPage.tsx`'s Participants card gained an "Others" checkbox revealing a `Textarea`; `CalendarAgendaPage.tsx`'s event row appends it after the participant count (`"N participants + {otherParticipants}"`) when present. `createCalendarEventInternal` still requires at least one real participant — Others is additive, not a substitute.
- **Monthly Revenue gained an "All Outlets (Total)" option 2026-08-31**, for entering one company-wide figure by hand instead of nine per-outlet ones. `ALL_OUTLETS_ID = 'all'` (`src/features/hr/payroll/revenueService.ts`, mirrored as a local const in `functions/src/hr/payroll/recordMonthlyRevenue.ts` since it's a sentinel, not a real outlet — the server's `outletId in OUTLET_DEPARTMENTS` check now also accepts it explicitly). **Deliberately not wired into `ManningBudgetReportPage.tsx`'s existing revenue lookup**, which matches `` `${outletId}::${periodMonth}` `` against each cost row's own real `outletId` — an `'all'`-keyed row simply sits unused in that map, harmless but also not consumed. If the report should ever fall back to the company total when no per-outlet figure exists, that's a separate, not-yet-requested change.
- **Employee record gained eight fields 2026-08-31, reported as two requests** (remove the legacy `Address` box; add Place of Birth/BPJS Ketenagakerjaan/BPJS Kesehatan/Mother's Name as text and Blood Type/Marital Status/Tax Status/T-Shirt Size as dropdowns). Four of the eight — `motherName`, `bpjsTk`, `bpjsKesehatan`, `personalTaxStatus` — already existed on `Employee`, `createEmployee.ts`/`updateEmployee.ts`, and `EmployeeFormPage.tsx`'s own local state, but had **zero rendered input anywhere**: the 2026-08-29 bulk-import pass explicitly left them out as "dead UI" and nobody had built the single-employee form control either, so they were unreachable end to end despite being fully wired everywhere except the JSX. This pass only added the missing `<Input>`/`<Select>` — no type, backend, or state change for those four. The other four are genuinely new: `birthPlace` (free text), `bloodType` (A/B/AB/O), `maritalStatus` (`single`/`married`/`widowed` — deliberately 3 values, not the 4-value `'single'|'married'|'divorced'|'widowed'` that sat in `employee.types.ts`'s now-deleted PLANNED section; matches the candidate portal's own existing `MARITAL_STATUSES` in `functions/src/recruitment/portal/applicationForm.ts` exactly, "divorced" was never asked for by either), and `tshirtSize` (XS/S/M/L/XL/XXL) — new enums in `src/constants/hr.ts`, mirrored in `functions/src/hr/employees/helpers.ts`, validated the same way `religion`/`personalTaxStatus` already were (`updateEmployee.ts`'s `STRING_FIELDS` gained `birthPlace`; the other three got their own one-of blocks). All eight now render on `EmployeeFormPage.tsx`'s Personal card and display on `EmployeeProfilePage.tsx`'s Personal card, which showed none of them before. The legacy `Address` textbox is gone from create/edit — `Employee.address`, its backend handling, and the profile's own read-only `Field label="Address"` are all untouched, so an existing record's value is preserved (the update callable only touches a field present in the submitted `updates` object) and still visible on the profile; the form simply no longer collects or edits it. **Deliberately not done**: `EmployeeImportPage.tsx`'s CSV bulk-import template was not extended to cover any of the 8 fields, despite this repo's own "bulk import mirrors the single form" precedent from 2026-08-29 — an explicit scope cut for this pass, not an oversight. No `firestore.rules` or permission-string change — every field here is gated by the record's existing `employees.create`/`employees.update`, unchanged. `npm run build`, `npm --prefix functions run build`, and `npm run check` (27/27) all green; not exercised against the emulator.

## Documentation is the source of truth

`docs/` (core/, modules/, platform/) holds the authoritative product/architecture specs, and the code is written to trace back to them. Comments cite sections directly, e.g. `// API.md §7`, `// DATABASE.md §23`, `// RBAC.md §4`. **Before changing behavior in a module, read its doc** — and when code and doc disagree, the doc usually reflects intent while the code reflects what's actually shipped.

Docs like `docs/core/ARCHITECTURE.md`, `API.md`, and `RBAC.md` describe the Firebase design (Firestore, Cloud Functions, custom claims, security rules), which is what's actually running. The one stale detail to watch: they place `firebase.json`/`firestore.rules`/`storage.rules` under `src/`, and those now live at the repo root.

If a doc and `package.json` ever disagree, trust `package.json` and this file.

## Architecture

### Client reads, actions write

The single most important rule: **clients never write to Firestore directly.** Every mutation goes through a named Cloud Function; `firestore.rules` enforces this literally — every collection is `allow write: if false`, so the only writer is the Admin SDK. Clients read Firestore directly, governed by the read half of those rules (API.md §19).

- **Frontend → backend:** call callables via `callFunction<TResponse>(name, payload)` ([src/services/api/callFunction.ts](src/services/api/callFunction.ts)), which wraps `httpsCallable` and converts failures into `ApiError` ([src/services/api/errors.ts](src/services/api/errors.ts)) so callers never handle a raw `FunctionsError`. Reads go through [src/services/firestore/queries.ts](src/services/firestore/queries.ts) (`getDocument`/`queryDocuments`) and `subscriptions.ts` (`subscribeToDocument`/`subscribeToCollection`, real `onSnapshot` listeners). Both map snapshots through `normalizeTimestamps`, so every document arrives with ISO-string dates.
- **Backend callable shape:** every callable follows one skeleton — auth check, permission check, validate, mutate, audit, respond. Written out in [functions/CLAUDE.md](functions/CLAUDE.md).
- **`REGION` is `asia-southeast2` (Jakarta)** — set in both `functions/src/lib/admin.ts` and [src/services/firebase/functions.ts](src/services/firebase/functions.ts). These must match or every call fails with `NOT_FOUND`.

### RBAC

Three layers, and they are not interchangeable:

1. **Cloud Functions are the enforcement layer.** `requireActiveUser` re-reads `users/{uid}` and `roles/{roleId}` on every call, so permissions come from live Firestore, never from the caller's token. `requirePermission`/`requireAnyPermission` gate the rest.
2. **`firestore.rules` gates reads only** (every collection is write-denied). Rules read `request.auth.token.role/departmentId/outletId` — custom claims kept in sync with the user doc by `functions/src/auth/syncUserClaims.ts`. Reading claims off the token rather than a `get()` keeps rule evaluation cheap, but it means a role change only reaches the rules after the claim syncs and the client refreshes its token. **Covered by `npm run test:rules`** (`functions/test/rules.mjs`, added 2026-08-29): 86 tests over the sensitive branches — write-denial on every collection including for superAdmin, the `isIssued`/`isApproved` approval gates, the appraisal-recommendation self-exclusion, `disciplinaryActions`' three branches, and outlet/department scoping. It mints unsigned JWTs against the Firestore emulator's REST API rather than adding `@firebase/rules-unit-testing`, so it needs no dependency and no Auth emulator. If you change a read rule, run it.

**A `list` is not a batch of `get`s, and this is the most common way to break a page here.** On a query, rules are evaluated against the *query*, not each document: `resource.data.x == y` must be **provable** from the query's own constraints, so a query that omits the constraint is denied *outright* rather than returning the subset that would have passed. A service that fetches broadly and filters client-side against an outlet/department-scoped rule therefore returns nothing at all. `rules.mjs` covers this by issuing the exact queries `src/` services send, each paired with an assertion that the unconstrained form is still denied. **This layer found two live bugs when it was added on 2026-08-29, both now fixed**: `equipmentService.subscribeToRegister` sent only `orderBy('assetCode')` and filtered outlet client-side, so the register was denied outright for every non-elevated user (and silently — it passed no `onError`); and Employee Communication's own-record scope filtered `employeeUid` without `releasedToEmployee`, which the rule also requires, so an employee never saw their own released records. **When you write an outlet- or department-scoped service function, put the scoping filter on the query, not in a client-side `.filter()`.** The client-side pass is fine only for fields no rule mentions.
3. **The client is UX only.** `AuthProvider` ([src/contexts/AuthProvider.tsx](src/contexts/AuthProvider.tsx)) listens via `onAuthStateChanged`, then holds a live `subscribeToDocument` on `users/{uid}` and loads `roles/{roleId}.permissions` into the Zustand auth store. `ProtectedRoute`/`RoleRoute` ([src/routes/](src/routes/)) and `PermissionGuard` read from that store — they hide UI, they don't secure anything.

### Shared "engines"

`functions/src/shared/` holds the cross-module engines every feature builds on: **approval** (`submitApproval`, `approveStep`, `rejectStep`, `returnForRevision`, `cancelApproval`, plus the `onApprovalRequestResolved` trigger), **tasks** (`createTaskInternal` and its callables), **notifications** (`sendNotificationInternal`, `notifyUsersByRole`), **file storage** (`createFileMetadata`/`deleteFile`, soft-delete only), and **calendar**. Prefer calling the `*Internal` helpers from another function over duplicating their logic — they own the audit and notification side effects.

### Frontend structure

Feature-first under [src/features/](src/features/)`<module>/` (pages, components, hooks, services per feature). The router ([src/routes/routes.tsx](src/routes/routes.tsx)) mounts unbuilt modules as `<ModulePlaceholder>`. `src/components/ui/` is a small shadcn-style primitive set (`components.json`, `class-variance-authority`, `tailwind-merge`); `src/components/shared/` is app-level shared components. Global state is Zustand stores in [src/store/](src/store/) (`authStore`, `themeStore`, `toastStore`, `uiStore`). Path alias `@/` → `src/` (configured in both `vite.config.ts` and `tsconfig.json`).

### Intentional duplication

`functions/` is a separate npm package with its own `tsconfig.json`, so it cannot import from `src/`. `functions/src/lib/collections.ts` and `functions/src/lib/permissions.ts` are hand-mirrored subsets of `src/constants/collections.ts` and `src/constants/permissions.ts`; both files say so in their header comments. Role→permission rows live in Firestore `roles/{roleId}` docs, not in any repo constant. When you change a collection name or permission string, **update both copies** — and remember `firestore.rules` hardcodes collection names and role names a third time.

### Frontend conventions worth keeping in mind

- UI is built from the primitives in `src/components/ui/` plus `src/components/shared/EmptyState`. **There is no Dialog/Modal** — multi-step or confirm flows are separate routed pages, not modals. `Button` has no `asChild`/Slot support — use `useNavigate()` + `onClick`, not `<Button asChild><Link>`.
- Static `/new`-style routes must be registered before `/:id` param routes in the same children array to avoid the param route swallowing them.
- Gotcha: a `const` narrowed by an early-return guard (`if (!source) return ...`) does **not** stay narrowed inside a closure (event handler, callback) defined later in the same component when the variable came from `useState` or `.find()` — TS re-widens it to possibly-`undefined` inside the closure. Extract a plain local (`const id = source.id`) right after the guard and reference that inside the closure instead of the original variable.

## Definition of done

A change is finished only when all of these hold:

1. `npm run build` passes at the root with **zero errors**. The tree is green; keep it green.
2. If you touched `functions/`: `npm --prefix functions run build` passes. It is outside the root `tsc` project, so a root build being green says **nothing** about whether `functions/` compiles.
3. If you added or renamed a collection or permission: update both `src/constants/` and the mirror in `functions/src/lib/`, and add the collection's `firestore.rules` block — a new collection with no rules block falls through to the deny-all match and reads silently fail.
4. Any new callable follows the canonical skeleton — `requireActiveUser` → `requirePermission` → validate (`AppError`) → mutate with `newDocumentBaseFields`/`updatedFields` → `recordAuditEvent` → `successResponse`, wrapped in `try/catch (error) { handleError(error) }` — is exported from `functions/src/index.ts`, and the client reaches it only through `callFunction`.
5. Behavior implemented from a spec carries a doc-section citation comment (`// API.md §7` style), matching the existing convention.
6. Any new query with an equality filter plus a range or `orderBy` on a different field has a matching entry in `firestore.indexes.json`. Firestore fails these at runtime, not build time, so the typecheck will not catch it.
7. You exercised the affected flow — `firebase emulators:start` plus `npm run dev` with `VITE_USE_FIREBASE_EMULATOR=true` is the safe way; there is no frontend test suite to lean on.
8. This file's "Current state" section is updated if your change altered any fact in it.
9. Commit messages follow Conventional Commits going forward: `type(scope): summary` (e.g. `feat(hr): ...`, `fix(approval): ...`, `docs: ...`). History before July 2026 is mixed — match the convention, not the history.

## Gotchas

- **A new collection with no `firestore.rules` block is unreadable, not open.** The deny-all `match /{document=**}` at the bottom catches anything that falls through, so the symptom is an empty list or a permission error in the console, not a security hole.
- **Missing composite indexes fail at runtime only.** Firestore rejects an unindexed equality+range/orderBy query with an error containing a URL that creates the index — but add it to `firestore.indexes.json` rather than clicking through, or it won't exist in the next environment.
- **`REGION` must match on both sides.** `functions/src/lib/admin.ts` and `src/services/firebase/functions.ts` both hardcode `asia-southeast2`; a mismatch surfaces as every callable failing with `NOT_FOUND`, which reads like a missing function rather than a config error.
- **Custom claims lag the user doc.** `syncUserClaims` updates the token claims `firestore.rules` reads, but an already-issued ID token keeps its old claims until it refreshes (~1h, or on a forced refresh). Cloud Functions are unaffected — `requireActiveUser` re-reads Firestore every call — so a role change takes effect for writes immediately but for reads only after the token turns over.
- Missing `VITE_FIREBASE_*` vars throw at module load from [src/services/firebase/config.ts](src/services/firebase/config.ts), listing exactly which are absent. If a deployed build fails on sign-in, check the deployment's env vars (e.g. Vercel project settings), not just `.env.local`.
- `strict` TypeScript is on for both the frontend and `functions/`; both builds fail on any type error.
- `npm run lint` fails with "eslint is not recognized" — eslint is not in `devDependencies` and there is no config file, even though the `lint` script itself references real eslint flags. `npm run build` (tsc + vite) plus `npm run check` are the quality gate until lint is actually scaffolded.
- `generateAppraisalInsights` needs an `ANTHROPIC_API_KEY` secret (`functions/src/lib/secrets.ts`, set via `firebase functions:secrets:set`). Without it the callable deploys but fails at call time.
