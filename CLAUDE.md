# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

NourishOS is an internal operational platform for Nourish Group Indonesia, a multi-outlet F&B company — a React SPA frontend backed by **Firebase** (Auth, Firestore, Cloud Functions, Storage). It is in early milestone development. Implemented today: **HR appraisal** (including AI insights), **HR employee database**, **HR recruitment**, **HR Inventory** (uniform & asset stock), **HR Reports**, **Security patrol/checkpoints**, **Operations** (Lost & Found, Incident Reports, Daily Updates), **Documents** (Job Descriptions, SOP Library), **Communications** (Announcements, Tasks), **Finance** (Expense Requests), the **executive Calendar**, **Settings** (Roles & Permissions), and the **dashboard** — frontend pages plus their Cloud Functions. Every other module (Reports) is mounted as a `<ModulePlaceholder>` route stub. **There is no top-level Purchasing, Inventory, or CRM module** — all three were cut from scope (2026-08-15); HR Inventory at `/hr/inventory` is the only inventory concept in the app (see "Current state of the tree").

PWA support (manifest, service worker) is **planned but not built** — there is no `vite-plugin-pwa` yet. Treat it as a plain Vite SPA today; don't claim or rely on offline/installable behavior until that lands.

## Environment

Development happens on Windows 11 with PowerShell as the shell. Write commands in PowerShell syntax (`;` chaining, `$env:VAR`, no heredocs into bash).

## Commands

Frontend (repo root):

```
npm run dev      # Vite dev server on http://localhost:5173
npm run build    # tsc (typecheck) then vite build — fails on any type error
npm run preview  # serve the production build
npm run lint     # ASPIRATIONAL — eslint is not installed and has no config; this fails today
```

Cloud Functions live in `functions/`, a separate npm package with its own build and emulator commands — see [functions/CLAUDE.md](functions/CLAUDE.md).

There is **no test runner configured** for the frontend — do not assume `npm test` exists or invent test commands. Verification means: `npm run build` passes at the root, and you exercised the affected flow in `npm run dev`.

Copy `.env.example` → `.env.local` and fill in the `VITE_FIREBASE_*` values from your Firebase project. [src/services/firebase/config.ts](src/services/firebase/config.ts) validates them at module load and throws listing exactly which are missing, so a misconfigured deployment fails loudly rather than deep inside the auth flow. Set `VITE_USE_FIREBASE_EMULATOR=true` to point the app at the local Emulator Suite instead of the live project.

## Current state of the tree (last verified 2026-08-12)

These are facts, not standards — the standards are in "Definition of done". **If your change alters any fact here, update this section in the same commit.**

- **Documents reach the app as ISO strings, not `Timestamp`s.** `queryDocuments`/`subscribeToCollection` take genuine `QueryConstraint`s, so `where`/`orderBy`/`limit` are pushed down to the server instead of filtering a whole fetched collection client-side. [src/services/firestore/normalize.ts](src/services/firestore/normalize.ts) converts `Timestamp` → ISO string at the single read boundary (recursively, for timestamps nested in maps/arrays), which is why `BaseDocument.createdAt` and friends are still typed `string` rather than reverting to `Timestamp` — components, format helpers, and `src/utils/date.ts` never see a `firebase/firestore` type. Cloud Functions still write real `serverTimestamp()`s.
- **`Employee` in `src/types/employee.types.ts` is the shipped shape** (mirrors what the old, now-unported `createEmployee` action used to write: `position`, `employmentStatus`, ISO-string dates). The PRD §12.1 schema (`TaxStatus`, `EmployeeCompensation`, etc.) sits below it in a clearly-marked PLANNED section — do not use those in shipped code paths yet.
- **Employee create/edit revised (2026-08-15): `preferredName` removed, Department/Outlet are real dropdowns, a Contract PDF slot, and bulk import.** `preferredName` is gone everywhere (type, both callables, form, CSV export, search, profile) — it had no other logic depending on it. Department/Outlet on `EmployeeFormPage.tsx` were plain free-text `<Input>`s (typo-prone, and `departmentId` is what `firestore.rules` uses to scope department-leader reads) — they're now cascading `<Select>`s mirroring `RegisterPage.tsx`'s `OUTLETS`/`OUTLET_DEPARTMENTS`/`optionsFor` pattern, with a legacy-value fallback so an existing bad value stays editable instead of silently blanking. `createEmployee.ts`/`updateEmployee.ts` now validate the pair against `OUTLET_DEPARTMENTS` server-side too — closing a gap `registerUser.ts` already covered but the employee callables never did. `EmployeeProfilePage.tsx` gained a distinct "Contract Document" card (`resourceType: 'employeeContract'`, PDF-only) alongside the pre-existing generic "Documents" card (`resourceType: 'employee'`) — same `FileUpload`/`FileList`/`createFileMetadata`/`deleteFile` mechanism, zero backend changes, since `resourceType` is just a string and `storage.rules`/`firestore.rules` are already resourceType-agnostic. **Bulk import** (`/hr/employees/import`, HR_OPERATIONS.md §9.1-F12, previously unbuilt anywhere in the app) follows the one existing precedent for reusing a callable's mutation logic from a second caller — `createCalendarEventInternal`'s extraction — so `createEmployee.ts` now exports `createEmployeeInternal`, and the new `importEmployees` callable loops it per row with partial-success semantics (a bad row is reported, not thrown, and the batch continues), capped at 100 rows. No permission-string mirror was duplicated for this: `src/utils/csv.ts` gained a hand-rolled `parseCsv` (no CSV library on either side, still none — this reads a template the app itself generates) and the import page resolves Department/Outlet/Gender/Employment Status/Contract Type from either their display label or raw value, since the template is filled in by hand.
- **Vite warns that the main JS chunk is >500 kB** — a warning, not a failure. Code-splitting is future work; don't treat the warning as a regression you introduced.
- **Design tokens migrated to v3 ("Basalt") on 2026-08-11.** `src/styles/globals.css` and `tailwind.config.ts` now carry the Basalt palette (Pandan `#0E4F47` primary, cool paper `#F4F5F3` bg, status colors = workflow ramp: success `#15803D`/warning `#B45309`/error `#B91C1C`/info `#1D4ED8`, radii unchanged 4/8/12/12, shadows re-tinted neutral cool instead of warm), fonts Archivo Variable + IBM Plex Mono via Google Fonts `<link>` in `index.html` (self-hosting via `@fontsource` deferred — no service worker exists yet to justify it, see `ponytail:` comment there), `Button`'s secondary variant is now bordered/sunken neutral (dropped filled Deep Olive — two filled brand-ish buttons per screen violated Basalt's rationing rule), default `Button` height bumped 44px→48px (universal touch minimum). **`StatusPill` is built** (`src/components/ui/status-pill.tsx`) — generic, takes `{tone, icon, label}`, not coupled to any one status enum, so each module owns its own status→tone/icon mapping (`AppraisalStatusBadge` for `ApprovalStatus`; `INCIDENT_STATUS_TONE`/`_ICON` in `incidentFormat.ts` for `IncidentStatus`; `LOST_FOUND_STATUS_TONE`/`_ICON` in `lostFoundFormat.ts` for `LostFoundStatus`). Wired into HR appraisal, both Operations Incidents pages, both Lost & Found pages, and the Daily Updates task-aging badge, replacing the old `Badge`-based status rendering there (severity/value-tier badges are untouched — those aren't lifecycle state). **Not yet migrated**: the field/desk density switch (`data-density` attribute, viewport-auto per D1) doesn't exist in code — components are still single-density; `ApprovalCard`/`AuditTimeline`/`TaskRow`/`MetricTile`/`OutletBadge`/`QueueBanner` (STYLE_GUIDE.md § Shared components) are still unbuilt; the offline queue UX (D3) has no implementation. Dark mode (`.dark` class in globals.css) has Night pass hex values wired but no role-based-default trigger (D2) implemented — it's manual-toggle-only today. See `docs/core/STYLE_GUIDE.md` v3 and `docs/2026-08-11-nourishos-design-system.md` for full spec and signed-off decisions.
- **`src/components/ui/` primitives are native-HTML, never Radix** — `Select`/`Input`/`Checkbox`/`Switch`/`Tabs`/`Tooltip` etc. style native elements rather than pulling in a headless-component dependency. Match that convention when adding one.
- **The HR recruitment pipeline is shipped end to end (2026-08-14), and `/hr` is now a hub.** The employee register moved from the `/hr` index to `/hr/employees`; the index is [src/features/hr/pages/HrHomePage.tsx](src/features/hr/pages/HrHomePage.tsx). Four chained sub-modules live under [src/features/hr/recruitment/](src/features/hr/recruitment/) and [functions/src/hr/recruitment/](functions/src/hr/recruitment/): requisitions (`recruitments`), candidates (`candidates`, stages ST-01…ST-08), interviews (`interviews`), onboarding (`onboardingChecklists`) — twelve callables plus an `hr/requisition` approval route. Things worth knowing before touching it: **a candidate cannot exist without an approved requisition** and **`vacancyStage` is only ever set by the approval-resolved handler** in `functions/src/hr/recruitment/index.ts`, which is what makes "no vacancy before approval" true rather than merely intended; hiring (ST-06) is the one write with side effects — it counts against the requisition inside a transaction, completes it when the last opening is filled, and generates the onboarding checklist; **`createCalendarEventInternal` was extracted** from `functions/src/shared/calendar/events.ts` so `scheduleInterview` can write a calendar event without a second callable (the callable now owns only auth + audit); `createEmployee` gained an optional `candidateId` that links the new employee back to the candidate and its checklist, since **hiring deliberately does not auto-create an employee** (candidate data has no NIK, contract type or probation). Deviations from `docs/modules/employee-requisition.md` and `HR_OPERATIONS.md` §9.4: the approval chain is fixed (hrManager → generalManager) rather than adding a Director step for unbudgeted requests — `budgeted` is captured, so that is a `shared/approval/routes.ts` change when wanted; no `confidential/compensation` subdocument; no WhatsApp (§9.5) since no Fonnte adapter exists; the board moves stages via a `<Select>` rather than drag-and-drop (no new dependency). `ROLE_PERMISSIONS` gained `recruitment.read`/`recruitment.create` for the leader roles — **that only affects role docs seeded from now on**, so existing `roles/{roleId}` documents need the strings added by hand.
- **Operations is shipped end to end.** Real pages under `src/features/operations/*/pages/` are mounted at `/operations/lost-found`, `/operations/incidents`, and `/operations` (Daily Updates feed), backed by `functions/src/operations/{lostFound,incidentReports,dailyUpdates}/` and their `firestore.rules` blocks. Three deliberate deviations from `docs/modules/{lost-and-found-report,incident-report,daily-updates}.md` carry over conceptually into whatever ports these next, made for consistency with what was already shipped rather than gaps: (1) none of the three store an `attachments: string[]` array inline — they follow the existing convention (Employee/Appraisal/PatrolLog) of querying `files` by `resourceType`/`resourceId` instead; (2) Incident Reports routes by `assignedToRole` (a role string) rather than an arbitrary uid picked from that role, and equipment-failure incidents get a `workOrders` doc written inline rather than a full Work Orders module being built for one field; (3) Daily Updates tags Task docs (`tags: ['dailyUpdate']`) instead of adding `TASK_TYPE.DAILY_UPDATE` to the shared enum, matching the "no module-specific task types" rule `src/types/task.types.ts` already documents — `daysOpen`/`escalationLevel`/`carryForwardFromTaskId` were added to `Task` as optional fields only. WhatsApp (Fonnte) notifications from the daily-updates.md notification matrix are out of scope — everything routes through the existing in-app `notifications` collection.
- **There are no `/demo/*` preview routes** — unbuilt modules are `<ModulePlaceholder>` stubs only. Two constants the old demo data owned live in the shipped format modules: `RETENTION_DAYS` in `lostFoundFormat.ts`, `INCIDENT_ROUTING` in `incidentFormat.ts`.
- **`docs/core/HR_OPERATIONS.md` is the HR & Ops PRD (v2.0.0)** — code cites its §9.x/§12.x numbering.
- **The executive Calendar is shipped, and its backend was written fresh (2026-08-13).** `src/features/calendar/` was built during the Apps Script era, so `createCalendarEvent`/`cancelCalendarEvent` never existed as Cloud Functions and `calendarEvents` had no rules block. Both callables now live in [functions/src/shared/calendar/events.ts](functions/src/shared/calendar/events.ts) — the path `calendar.types.ts` already pointed at. Notes: `startAt`/`endAt` are stored as Firestore `Timestamp`s (the client sends ISO strings; the read layer converts back), conflict detection filters the `endAt` side in memory because Firestore allows only one range-filtered field per query, and every event is written `syncStatus: 'skipped'` since Google Calendar push-sync (HR_OPERATIONS.md §14.4) isn't provisioned — a permanently-`pending` row would read as a stuck job.
- **The backend runs on WITA (`Asia/Makassar`, UTC+8), declared explicitly — never inherited.** **Never use `new Date().toISOString().slice(0, 10)` for "today"** — it is always UTC regardless of `TZ`, so between 00:00 and 08:00 WITA it silently returns yesterday and mis-files the day's records. Use the helpers in `functions/src/lib/timestamps.ts`; full rules in [functions/CLAUDE.md](functions/CLAUDE.md).
- **Self-service registration is shipped, against what `AUTHENTICATION.md` §5 says (2026-08-14).** The doc specifies HR-provisioned accounts and "users are **not** self-registered", but that provisioning flow has no UI or callable, so only the bootstrap superAdmin could ever sign in. `/register` ([src/features/auth/RegisterPage.tsx](src/features/auth/RegisterPage.tsx), public route) collects Full Name → Outlet → Department → Role, each dropdown narrowed by the one above it, and `registerUser` ([functions/src/auth/registerUser.ts](functions/src/auth/registerUser.ts)) writes `users/{uid}` with `status: 'active'`. `ProtectedRoute` now splits the two denial cases: **no profile → `/register`**, profile present but not active → `/unauthorized`. Notes: the org chart lives in `src/constants/organization.ts` mirrored by `functions/src/lib/organization.ts` (constants, not `outlets`/`departments` Firestore docs — nothing edits them and there is no Settings UI); `OUTLET_DEPARTMENTS` is *derived* from outlet concept type since no doc maps outlets to departments; `superAdmin` is absent from every `DEPARTMENT_ROLES` list so it can't be self-granted; and `registerUser` seeds `roles/{roleId}` from `ROLE_PERMISSIONS` (RBAC.md §4/§5) the first time a role is claimed, since only `roles/superAdmin` was ever created by hand.
- **The Documents module ships two sub-modules: Job Descriptions and the SOP Library (2026-08-14).** `/documents` is no longer a `<ModulePlaceholder>` — its index is a hub ([src/features/documents/pages/DocumentsHomePage.tsx](src/features/documents/pages/DocumentsHomePage.tsx)) with a card per sub-module. Job Descriptions lives at `/documents/job-descriptions` ([src/features/documents/jobDescriptions/](src/features/documents/jobDescriptions/)) with `new`, `access` and `:jobDescriptionId/edit` beneath it, backed by four callables in [functions/src/documents/jobDescriptions.ts](functions/src/documents/jobDescriptions.ts); the SOP Library is the same four routes and four callables one level over at `/documents/sop-library` ([src/features/documents/sopLibrary/](src/features/documents/sopLibrary/), [functions/src/documents/sopLibrary.ts](functions/src/documents/sopLibrary.ts)), against the `sops` collection with `departmentId`/`sopNumber`/`topic`/`driveUrl` and **no `roleId`** — an SOP belongs to a department, not a position. Content in both is curated by hand by the super admin, so the shipped state is two empty registers. Several firsts live here: **`systemSettings/jobDescriptionAccess` is the first runtime-editable config doc** (an allowlist of role ids written by `setJobDescriptionAccess`; `systemSettings/sopAccess` is its independent twin) and the reason `firestore.rules` now contains **its first cross-document `get()`** — module read access can't ride on a custom claim if it's editable at runtime. Both are matched by literal doc id so the sibling `*NumberSequences` counters stay in the deny-all, and before a doc exists its `get()` yields null and only `isElevated()` roles can read (closed by default, on purpose). The SOP Library also drops most of `documents.md` §6 — no version history, approval workflow, tags, categories, search or PDF preview; it is the browse-by-department register only, its departments come from `src/constants/organization.ts` (14) rather than §6's 10, and `sopNumber` is free text with no uniqueness check (one curator). **`requireSuperAdmin` ([functions/src/lib/rbac.ts](functions/src/lib/rbac.ts)) is the first role-based server gate** — every other callable checks permission strings, but `superAdmin` is absent from `ROLE_PERMISSIONS` and its role doc was hand-seeded, so a new `jobDescriptions.manage` string would 403 on a fresh install. Two deviations: `docs/modules/documents.md` §4/§20 has no Job Descriptions section or collection, and the PDF is an **external URL on the doc** rather than a `files` record queried by `resourceType`/`resourceId` (the PDFs already live in the company's Drive; nothing is uploaded to Storage). The link is `new URL()`-parsed server-side and restricted to http(s) because it renders into an `<a href>`.
- **Communications ships Announcements and Tasks, and `users` is now readable by every signed-in user (2026-08-15).** `/communications` is a hub ([src/features/communications/pages/CommunicationsHomePage.tsx](src/features/communications/pages/CommunicationsHomePage.tsx)); Announcements lives at `/communications/announcements` with `new`, `:announcementId` and `:announcementId/edit`, backed by five callables in [functions/src/communications/announcements.ts](functions/src/communications/announcements.ts), and Tasks at `/communications/tasks` (`new`, `:taskId`) is **frontend-only** over the Task Engine that had shipped with no surface at all — `addTaskComment` ([functions/src/shared/tasks/taskComments.ts](functions/src/shared/tasks/taskComments.ts)) is the one callable it added, and `createTask`/`assignTask`/`completeTask`/`cancelTask` keep their creator-or-assignee authorization rather than gaining `requirePermission` checks (five internal callers depend on them). Things worth knowing before touching it: **`audienceUids` is the load-bearing field** — `firestore.rules` fails a whole list query if one returned document fails its read rule, and "outletIds is empty OR contains mine" is not expressible as a query, so `publishAnnouncement` resolves outlet/department/role targeting to concrete uids once and both the rule and the feed query key off that array (drafts have it empty, archiving clears it, which is what removes an announcement from every feed); `taskComments` denormalises `taskParticipants` for exactly the same reason. **Broadcast (`communications.md` §13) is not a separate collection or callable** — it is `category: 'emergency'` plus `publishNow`, gated by the new `announcements.broadcast` permission. **The `users` read rule widened from HR-and-above to any signed-in user** so a department leader can use the task assignee picker (§19 says they may assign tasks); the doc holds only work email, name, role, department, outlet and status. `subscribeToParticipantOptions` moved out of `calendarService.ts` into [src/services/shared/userService.ts](src/services/shared/userService.ts) as `subscribeToDirectory`, since two features now need it. New permissions `announcements.create`/`announcements.broadcast` were added to `ROLE_PERMISSIONS`, so — as with recruitment — **existing `roles/{roleId}` docs need the strings added by hand, and `roles/superAdmin` needs all three or a super admin gets 403 on create/publish.** Dropped from `communications.md`: Team Chat, Direct Messages, Activity Feed, Mentions and Communication Settings (their collections stay in the deny-all); the §9 Notification Center, because `NotificationBell`/`NotificationPanel` already cover it; §5's rich-text editor (plain text, `whitespace-pre-wrap`), its optional Review step, and scheduled publication; individual-user audience targeting; and §6's Accept/Waiting/Verified/Closed task stages, which the Task Engine still doesn't persist.
- **The dashboard is no longer a placeholder, and it carries the app's first approval queue (2026-08-15).** `/` is [src/features/dashboard/DashboardPage.tsx](src/features/dashboard/DashboardPage.tsx) — the `dashboard.md` §6 welcome banner plus four widgets in `src/features/dashboard/widgets/`: Pending Approvals (§9), Assigned Tasks (§10), Announcements (§12), and Upcoming Calendar (2026-08-15, closing the §26 "Company calendar" wishlist item — a thin wrapper over the calendar module's existing `subscribeToUpcomingEvents`, confirmed events only, no new query shape). All read-only, top five, linking out to the pages that own the actions; **no backend changes at all** — §17's `getDashboardSummary()`/`getPendingApprovals()` aggregators are not built, since every source already has a client-readable query. The one thing worth knowing: **the approval engine routes by role, not by uid.** The live step doc carries `approverRole` (`functions/src/shared/approval/submitApproval.ts`), there is no assignee field anywhere, and the equivalent on the request (`steps[currentStepIndex].approverRole`) is an array-index lookup Firestore cannot query — so `subscribeToApprovalQueue` ([src/services/shared/approvalService.ts](src/services/shared/approvalService.ts)) queries `approvalSteps` (`allow read: if isSignedIn()`) and joins each step back to its request one read at a time. **That join only works because every `approverRole` in `routes.ts` is on the `approvalRequests` read rule's elevated list — naming an approver role outside it means updating `firestore.rules` in the same change.** superAdmin gets a separate branch: it is an override approver but never a named `approverRole`, so it queries every pending step instead and sorts client-side (no second index). Two indexes were added (`approvalSteps` approverRole+stepStatus+createdAt, `approvalRequests` requestedBy+createdAt), and `ApprovalRequest.workflowId` became optional while gaining the `steps` array — the server writes the latter and never the former, so the type had it exactly backwards. Not built: KPI cards (§7), quick actions (§8), activity feed (§13), department/per-role variants (§14/§15), the §11 notifications widget (the header bell already is one), and §18's manual refresh (every widget is a live listener).
- **Finance ships Expense Requests, and approval routes are now conditional (2026-08-15).** `/finance` is the expense register itself rather than a hub ([src/features/finance/expenses/](src/features/finance/expenses/)), backed by five callables in [functions/src/finance/](functions/src/finance/): create/update (draft, owner-only) → submit → the resolved handler → `markExpensePaid` → `closeExpenseRequest`. The engine change is the part with reach: **`APPROVAL_ROUTES` values may now be a `(context) => steps` function** — the signature `routes.ts` always said it would grow — and `finance/expenseRequest` is the first one ([functions/src/finance/expenseSteps.ts](functions/src/finance/expenseSteps.ts), ≤5M → department leader + finance, >5M → + generalManager + director). `SubmitApprovalInternalInput` gained an optional `context`; **it is assembled by the callable from the stored document, never from `request.data`** — `totalAmount` decides the chain's length, so a client-supplied total would be a client-chosen approval chain. The three HR routes stay static arrays, and the `hr/requisition` `budgeted` branch that was deferred is now a one-file change. Other things worth knowing: "Department Manager" isn't a role, so step 1 resolves from the requester's department via `DEPARTMENT_ROLES`, then the chain drops the requester's own role (self-approval is blocked by uid, so a lone kitchenLeader would stall their own request), dedupes, and floors at `generalManager`; `buildExpenseApprovalSteps` is pure and pinned by `node functions/test/expense-steps.mjs`, which needs no emulator. **`functions/src/lib/sequences.ts` is new** — `allocateYearlyNumber` was copy-pasted four times; recruitment now shares it (same doc ids and field keys, no migration), while the incident and lost & found allocators keep their own because their counter field key differs. New permission `expenseRequests.pay`, granted to `finance` only so a GM who approved cannot also disburse; `requireExpensePermission` follows recruitment's `requireRecruitmentPermission` in letting superAdmin through, so no hand-seeding is needed this time. Deviations from `docs/modules/expense-request.md` (all now corrected in that doc): items embed on the request rather than an `expenseItems` collection, receipts are `files` records queried by `resourceType`/`resourceId` rather than a `receiptFileIds` array, and resolution is an eventually-consistent trigger rather than part of the engine's transaction.
- **HR Inventory ships uniform & asset stock tracking at `/hr/inventory` (2026-08-15), a 5th card on the HR hub.** `docs/modules/hr.md` §12 "Employee Assets" names `employeeAssets` and an Assign/Transfer/Return-per-asset shape; this ships a **unified stock ledger** instead — item master + per-outlet (per-size, when the item tracks sizes) stock levels + an append-only movement log — because uniforms need size-level quantities a per-asset-unit model doesn't represent, and "stock tracking, movement, cost" is the ledger pattern, not an assign/return log. **`EMPLOYEE_ASSETS` stays reserved and unused.** Deliberately distinct collections — `hrInventoryItems`/`hrStockLevels`/`hrStockMovements` in [functions/src/hr/inventory/](functions/src/hr/inventory/) — from the `INVENTORY_ITEMS`/`STOCK_LEVELS`/`STOCK_MOVEMENTS`/etc. that were reserved in `src/constants/collections.ts` for a separate, much larger F&B warehouse module `docs/modules/inventory.md` used to describe (FIFO/FEFO, waste logs). **That top-level module — and Purchasing and CRM alongside it — was cut from scope on 2026-08-15**: the reserved collection names, the `/inventory`, `/purchasing` and `/crm` `<ModulePlaceholder>` routes, their `Sidebar`/`ROUTES` entries, and all three PRD docs were deleted; HR Inventory is now the only inventory concept in the app. Five callables (`createInventoryItem`, `updateInventoryItem`, `receiveStock`, `issueStock`, `transferStock`) share one transactional helper, `applyDelta` (pinned by `node functions/test/hr-inventory-stock.mjs`, no emulator needed) — every mutation runs inside `db.runTransaction`, and `transferStock` reads both outlets' stock levels before writing either (Firestore transactions forbid reads after writes), writing two linked movement docs (`transferOut`/`transferIn`) sharing a `linkedMovementId`. `category` and `hasSizes` are immutable after item creation — changing either would orphan existing `hrStockLevels` docs keyed by the old size set. No per-serial asset tracking (a laptop is a count of "laptop" stock, not an individually tagged unit) and no approval engine — permission-gated like Operations, not multi-step approved like Finance. New permissions `hrInventory.manage` (item-master curation, `hrManager` only) and `hrInventory.record` (day-to-day receive/issue/transfer, granted to `hrManager` and every outlet leader via the shared `LEADER` array) — same standing caveat as every prior module: `ROLE_PERMISSIONS` only seeds a role doc on first claim, so existing `roles/{roleId}` docs (including `roles/superAdmin`) need both strings added by hand.
- **HR ships a Reports sub-module at `/hr/reports` (2026-08-15), a 6th card on the HR hub — eight report views, zero backend changes.** Active Employee, Resigned Employee, Employee Turnover, Manning Budget, Manning Cost, Employee Activity, Budget and Cost, and Upcoming Activity and Budget (`docs/modules/hr.md` §16 names most of these as aspirational; Manning Budget/Cost/Budget-and-Cost/Upcoming are net-new, not in that list). Every report is a client-side aggregation over collections the HR sub-modules already write (`employees`, `recruitments`, `interviews`, `onboardingChecklists`, `employeeActivities`, `hrStockMovements`) via new unfiltered/lightly-filtered subscriptions added alongside the existing ones (`subscribeToAllEmployeeActivities`, `subscribeToAllStockMovements`, `subscribeToAllInterviews`) — same "single subscription, filter client-side" convention `subscribeToEmployees`/`subscribeToAllStockLevels` already use, so no new Firestore indexes. **No new permission string** — a report is a different view over data the viewer's existing role can already read via `firestore.rules`, so there's no `hrReports.read`; pages that read `recruitments`/`interviews` (Manning Budget, Budget and Cost, Upcoming Activity and Budget) surface the same role-gated "Access restricted" state `RequisitionListPage` already does when that subscription errors. The one real gap: **Manning Cost and Budget and Cost are uniform/asset procurement spend from `hrStockMovements.totalCost`, not payroll** — `Employee` still carries no salary/compensation fields (`EmployeeCompensation` is PLANNED-only, employee.types.ts), so there is no payroll cost anywhere in the app; this was a deliberate scope call, not an oversight, confirmed before building. Shared additions worth knowing about elsewhere: `src/utils/csv.ts` (`toCsv`/`downloadCsv`) is now the one CSV serializer — `employeeExport.ts` was refactored to call it instead of hand-rolling escaping — and `src/components/shared/ReportTable.tsx` is a minimal column-config table (no sorting/pagination built in, filtering stays page-local) available to any future report-shaped page.
- **Settings ships Roles & Permissions at `/settings` (2026-08-15), closing a gap every module since HR Inventory has had to flag by hand.** `roles/{roleId}` docs used to be written exactly once — seeded from the server-only `ROLE_PERMISSIONS` map (`functions/src/lib/organization.ts`) the first time a role was claimed by `registerUser` — so every new permission string needed manual Firestore edits to reach existing role docs (including `roles/superAdmin`). The one new callable, `updateRolePermissions` ([functions/src/settings/updateRolePermissions.ts](functions/src/settings/updateRolePermissions.ts)), fixes that: it upserts a role's `permissions` array, following the exact `setJobDescriptionAccess`/`setSopAccess` upsert shape. **Gated by role identity (`requireSuperAdmin`), not a permission string** — `PERMISSIONS.ROLES_MANAGE` stays unused on purpose, since a permission string that gates editing permissions would be circular. `permissions` is validated for shape only (non-empty strings, deduped) rather than against a full whitelist — `functions/src/lib/permissions.ts` is a deliberate *subset* mirror (only strings actually `requirePermission`-checked server-side), not the complete ~50-string catalog that lives in `src/constants/permissions.ts`; growing it into a full mirror for this one caller would be new duplication with no other consumer, so `requireSuperAdmin` is the trust boundary instead. `firestore.rules` for `roles/{roleId}` needed no change (`allow read: if isSignedIn(); allow write: if false` already covered it — Admin SDK writes aren't rules-checked anyway). [src/features/settings/pages/RolePermissionsPage.tsx](src/features/settings/pages/RolePermissionsPage.tsx) is gated by `useRole().isRole('superAdmin')` rather than `PermissionGuard`, same reasoning; it groups `PERMISSIONS` (`src/constants/permissions.ts`) by module at render time instead of a hand-maintained grouping map, so new permission strings show up automatically. **This manages what a role can do, not who has which role** — there is still no admin UI to assign a role to a specific account (`changeRole`/`assignOutlet`/`activateUser` etc. from `AUTHENTICATION.md` §18 remain unbuilt); granting someone `superAdmin` for the first time is still a direct Firestore/Console operation outside the app.
- **superAdmin now bypasses every permission-string check, not just the callables that call `requireSuperAdmin` directly (2026-08-15).** `requirePermission`/`requireAnyPermission` (`functions/src/lib/rbac.ts`) return early when `user.roleId === 'superAdmin'`, and `AuthProvider.tsx` mirrors it client-side (`setPermissions(Object.values(PERMISSIONS))` instead of reading `roles/superAdmin`'s Firestore doc when the signed-in user is superAdmin). This closes the exact gap `requireSuperAdmin`'s own doc comment already named: superAdmin is deliberately absent from `ROLE_PERMISSIONS` and its `roles/superAdmin` doc was seeded by hand once, so every permission string added by every module since (there have been many — see every "existing role docs need the string added by hand" caveat elsewhere in this file) needed a manual Firestore edit to actually reach it, and evidently never fully did. Read access needed no change — `firestore.rules`' `isElevated()`/`hasAnyRole([...])` blocks already include `superAdmin` consistently across every collection (spot-checked the whole file). The Settings > Roles & Permissions page still reads/writes `roles/superAdmin`'s real permissions array for visibility/audit purposes; it's just no longer what enforcement depends on.
- **HR_OPERATIONS.md gap-closing pass (2026-08-15): employee polish, recruitment analytics, calendar improvements, and a Contract/Probation Tracker.** A full read of `docs/core/HR_OPERATIONS.md` against the codebase found its §16 roadmap stale (it describes Calendar/Daily Updates/Operations as unbuilt; all three had shipped) — the per-requirement `9.x-Fxx` tables cross-referenced against code were used instead. Out of scope, flagged rather than built: WhatsApp/Fonnte (no adapter, needs a live Fonnte account), Google Calendar sync (needs a Google OAuth app), the §6.4 weighted-component appraisal scoring model (an unresolved product decision the doc itself flags), and Disciplinary Actions/Training as full modules (the doc doesn't actually spec either beyond a reserved collection name).
  - **Employee polish**: `preferredName`-adjacent gaps closed — birth-month/religion filters and a birth-date sort on `EmployeeListPage.tsx`/`employeeSort.ts`; `disciplinaryType`/`disciplinaryStartPeriod`/`disciplinaryEndPeriod`/`recognitionType`/`recognitionPeriod` added to `Employee` (`DisciplinaryType` promoted out of `employee.types.ts`'s PLANNED section into `src/constants/hr.ts`, mirrored in `functions/src/hr/employees/helpers.ts`); a "Change History" card on `EmployeeProfilePage.tsx`. That last one needed a new callable, not a direct Firestore read — `auditLogs` is `allow read, write: if false` in `firestore.rules`, closed even to elevated roles per `AUDIT_LOG.md` §8, so [functions/src/hr/employees/getEmployeeAuditLog.ts](functions/src/hr/employees/getEmployeeAuditLog.ts) is the one narrow read-only exception in the app to "clients read Firestore directly" — gated by `EMPLOYEES_UPDATE`, returns entries, never mutates. New composite index: `auditLogs` resourceType+resourceId+timestamp.
  - **Recruitment analytics**: `timeToHireDays()` in `recruitmentFormat.ts` (derived from `stageHistory`, same style as the existing `daysInStage()`), shown on `CandidateDetailPage.tsx` for hired candidates. A 9th HR Report, **Recruitment Funnel** (`/hr/reports/recruitment-funnel`), follows the other eight's shape exactly — client-side aggregation, no backend changes — plus a "Print / Save as PDF" button using `window.print()` and a `print:` Tailwind variant rather than a new PDF library (none exists anywhere in the app, and browser print-to-PDF covers "export to PDF" natively).
  - **Calendar**: type/department/outlet/owner filters on `CalendarAgendaPage.tsx` (owner resolved via the existing `subscribeToDirectory`); a Month view (`CalendarMonthView.tsx`, new) behind an Agenda/Month `Tabs` toggle, backed by a new `subscribeToEventsInRange()` in `calendarService.ts` — same single-range-field shape `subscribeToUpcomingEvents` already uses, so no new index. **`companyEvent`-type events now route through the Approval Engine before they're confirmed** — the first real design change of the four: `CalendarEventStatus` gained a `'pendingApproval'` value and `CalendarEvent` gained `approvalRequestId`, `createCalendarEventInternal` (`functions/src/shared/calendar/events.ts`) submits via `submitApprovalInternal` when `eventType === 'companyEvent'` instead of writing `eventStatus: 'confirmed'` directly, a new `'calendar/companyEvent'` route (single-step `generalManager`) sits in `shared/approval/routes.ts` next to the previously-unused `'hr/contract'` route, and `functions/src/shared/calendar/index.ts` registers a `registerApprovalResolvedHandler('companyEvent', ...)` mirroring `hr/recruitment/index.ts`'s `'requisition'` handler exactly. No `firestore.rules` change — the existing read predicate doesn't discriminate by `eventStatus`.
  - **Contract/Probation Tracker** (in-app only — the WhatsApp delivery leg stays out of scope): [functions/src/hr/employees/contractAlerts.ts](functions/src/hr/employees/contractAlerts.ts), a new `onSchedule` job (06:00 WITA) implementing §14.1's threshold algorithm — contract expiry at 90/60/30/14/7/0 days, probation ending at 30/14/7 — via an exact-day-match Firestore query (`where(field,'==',targetDate)`, two equality filters, no new index needed) rather than fetching every employee and computing date diffs. **Deliberately stateless**: no `sentFlags` dedup map like the doc's own spec, since each threshold day matches exactly once as time passes — a real, noted simplification, not an oversight. Recipients escalate with `notifyUsersByRole`: `hrManager` always, the employee's own department leader (`DEPARTMENT_ROLES[departmentId][0]`, same lookup Finance's `expenseSteps.ts` uses) from 30 days out, `generalManager` from 7 days out.
- **FEATURE_SPECIFICATIONS.md gap-closing pass, round 2 (2026-08-15): Global Search, Work Orders, Opening/Closing Checklists, Company Forms + Templates, Disciplinary Records, Dashboard KPI cards.** A second, coarser PRD than `HR_OPERATIONS.md` — most of it maps onto already-shipped modules under different names or documented deviations (Google Sign-In is in fact the sole auth provider; Employee Assets stays superseded by the HR Inventory stock ledger). Six genuine gaps were built; **not built and flagged as separate, larger candidates**: Internal Chat (already explicitly deferred in `communications/README.md`), Departments/Outlets/Company Settings/Themes as Firestore-editable admin UI (`organization.ts` stays a hardcoded constant — making it runtime-editable would touch every cascading-dropdown call site), and HR Training (zero spec depth beyond a reserved collection name).
  - **Global Search**: the header `SearchBar` was a disabled placeholder — it now navigates to `/search?q=…` on Enter. [src/services/shared/searchService.ts](src/services/shared/searchService.ts) fires five independent Firestore prefix-match range queries (Employees by `fullName`, SOPs by `topic`, Job Descriptions by `title`, Announcements by `title`, Tasks by `title`), each wrapped so a rules-denied collection contributes zero results rather than failing the whole search — no new indexes (a same-field range+orderBy is auto-indexed). Work Orders isn't in the sweep yet (built same pass); "Reports" from the spec's list are aggregation pages, not records, so excluded.
  - **Work Orders**: `functions/src/operations/workOrders/` closes a comment in `createIncidentReport.ts` that literally said "no Work Orders backend exists yet." `createWorkOrderInternal` (same extraction precedent as `createCalendarEventInternal`) is now the one write path — the incident report's inline doc write was replaced with a call to it. Lifecycle `open → assigned → inProgress → completed → closed` is one `updateWorkOrderStatus` callable gated by a different permission per transition (`workOrders.assign`/`.update`/`.complete`, mirroring `expenseRequests.pay`'s separation of approving vs disbursing) — those three permission strings already existed in `src/constants/permissions.ts` and were already granted to `engineering`/`barLeader` in `ROLE_PERMISSIONS`, just never enforced server-side until now (the mirror was missing from `functions/src/lib/permissions.ts`). `/operations` became a hub (`OperationsHomePage.tsx`) once Work Orders and Checklists landed alongside the existing three sub-modules — same precedent HR and Documents already set; Daily Updates moved from the bare index to `/operations/daily-updates`.
  - **Opening/Closing Checklists**: item lists are a hardcoded code table (`src/constants/checklist.ts` / `functions/src/lib/checklist.ts`), not an admin-editable doc — the spec asks for completion tracking and photo attachments, not a template-authoring UI. One doc per outlet+day with a **deterministic id** (`${outletId}__${date}`), so there's no query and no composite index; `saveChecklistProgress` toggles one item at a time as staff work through the list. Photo attachments are one shared `FileUpload` for the whole day's checklist rather than per-item — a deliberate scope-down. New permission `checklists.record`, added to the shared `LEADER` array and `hrManager` (existing `roles/{roleId}` docs need it added by hand, same standing caveat as every module before this).
  - **Company Forms + Templates**: both are curated "title + external link" registers, same shape as SOP Library, so they share one collection-picking implementation ([functions/src/documents/resources.ts](functions/src/documents/resources.ts), `resourceKind: 'form' | 'template'`) instead of duplicating SOP Library's four-callable shape twice. Gated by `documents.publish` — an existing permission string with no caller until now (already granted to `hrManager`/`marketing`). Unlike Job Descriptions/SOPs there's no runtime access-list doc; every signed-in user can read. The spec's "Approval Workflow" sub-feature for Company Forms would mean a generic dynamic form-submission engine — out of scope, noted as a deviation, same kind of scope-down SOP Library already made against its own spec.
  - **Disciplinary Records**: reverses a prior deliberate scope-down — `HR_OPERATIONS.md` only asked for the 3 status fields already on `Employee`, but `FEATURE_SPECIFICATIONS.md` names Warning/Suspension Records and Investigation Notes as their own feature. `disciplinaryActions` (reserved, previously unused) is now the detail layer behind those fields — additive, **no auto-sync** back onto `Employee.disciplinaryType`/Start/End (HR sets both independently, a deliberate simplification). Investigation notes are an append-only array (`FieldValue.arrayUnion`, timestamped client-side since `serverTimestamp()` can't sit inside `arrayUnion`). Gated by the existing `employees.update` — no new permission string. Read is HR/elevated-only in `firestore.rules`, intentionally without the department-leader branch `/employees` has (would need a `get()` to the employee doc for `departmentId`, and this is more sensitive than the base record).
  - **Dashboard KPI Cards**: `src/components/shared/MetricTile.tsx` is the one shared component `STYLE_GUIDE.md` names as still unbuilt — built here rather than a one-off. `KpiCardsRow.tsx` reuses the four widgets' own subscriptions for 3 of 4 tiles (Pending Approvals, Open Tasks, Announcements — zero new reads); Active Employees is the one new read, a single `getCountFromServer` aggregation (no documents fetched, no listener).
- **Revision pass (2026-08-15): a religion dropdown, outlet/department become overridable on four Operations/Finance forms, and Work Orders gain photo attachments.** Employee `religion` was a free-text `<Input>` on both client and server — it's now a real enum, `RELIGION`/`RELIGION_LABELS` in `src/constants/hr.ts` (mirrored `RELIGIONS` in `functions/src/hr/employees/helpers.ts`): `hindu`/`christian`/`catholic`/`islam`/`other`, the 5 values actually wanted (a first pass shipped Indonesia's fuller 6-religion KTP list, corrected down to this set same-day). The incomplete `Religion = 'hindu' | 'christian' | 'muslim'` PLANNED type in `employee.types.ts` is gone, superseded by this. `EmployeeFormPage.tsx`'s religion `<Select>` carries the same legacy-value fallback the Department/Outlet dropdowns already established (an existing free-text value, or a value from the first-pass 6-religion list, stays selectable rather than blanking); `EmployeeImportPage.tsx`'s CSV resolver and `EmployeeListPage.tsx`'s filter both moved onto the fixed enum too.
  - **Lost & Found, Incident Reports, Daily Updates, Expense Requests all previously derived `outletId`/(`departmentId`) purely server-side from the caller's own profile** (`outletId: user.outletId`, "never from input," per `createIncidentReport.ts`'s and `createExpenseRequest.ts`'s own comments) — correct for the common case, but wrong for staff who work across outlets (drivers, roving security/HR, multi-outlet managers). All four now accept an **optional override** from the client, validated server-side against the org chart (`OUTLET_DEPARTMENTS`, the same source `registerUser`/`createEmployee` already validate against) and defaulting to the caller's own when omitted — not a blank trust boundary, a validated one. Daily Updates' outlet/department fields flip from read-only display text to a real cascading `<Select>` pair; the other three gain a new outlet (Lost & Found, Incident Reports) or outlet+department (Expense Requests) field that wasn't on the form at all before. `ExpenseRequest.outletId`/`departmentId` turned out to already be typed — inherited from `BaseDocument`, just not re-declared — so no type gap actually existed there, only under-documentation, now fixed with a comment instead of a redundant re-declaration.
  - **Work Orders** (shipped same day as part of the FEATURE_SPECIFICATIONS.md pass above) gained a "Photos" card on `WorkOrderDetailPage.tsx` — `FileUpload`/`FileList` against `resourceType: 'workOrder'`, the same generic `files` convention every other module uses; no backend or rules change, since `firestore.rules`'/`storage.rules`' `files` blocks are already resourceType-agnostic.
  - Left for a later pass, explicitly deferred by request: a Communications sub-module for issuing a formal per-employee notice tied to a Disciplinary Record (distinct from the HR-only `disciplinaryActions` records already shipped) — scope not yet decided.
- **Docs mark unshipped functions/collections/components as "Planned"** and describe the Firebase design, which is what runs. One stale detail: they place Firebase config under `src/`, and it's at the repo root now.

## Documentation is the source of truth

`docs/` (core/, modules/, platform/) holds the authoritative product/architecture specs, and the code is written to trace back to them. Comments cite sections directly, e.g. `// API.md §7`, `// DATABASE.md §23`, `// RBAC.md §4`. **Before changing behavior in a module, read its doc** — and when code and doc disagree, the doc usually reflects intent while the code reflects what's actually shipped.

Docs like `docs/core/ARCHITECTURE.md`, `API.md`, and `RBAC.md` describe the Firebase design (Firestore, Cloud Functions, custom claims, security rules), which is what's actually running. The one stale detail to watch: they place `firebase.json`/`firestore.rules`/`storage.rules` under `src/`, and those now live at the repo root.

If a doc and `package.json` ever disagree, trust `package.json` and this file.

## Architecture

### Client reads, actions write

The single most important rule: **clients never write to Firestore directly.** Every mutation goes through a named Cloud Function; `firestore.rules` enforces this literally — every collection is `allow write: if false`, so the only writer is the Admin SDK. Clients read Firestore directly, governed by the read half of those rules (API.md §19).

- **Frontend → backend:** call callables via `callFunction<TResponse>(name, payload)` ([src/services/api/callFunction.ts](src/services/api/callFunction.ts)), which wraps `httpsCallable` and converts failures into `ApiError` ([src/services/api/errors.ts](src/services/api/errors.ts)) so callers never handle a raw `FunctionsError`. Reads go through [src/services/firestore/queries.ts](src/services/firestore/queries.ts) (`getDocument`/`queryDocuments`) and `subscriptions.ts` (`subscribeToDocument`/`subscribeToCollection`, real `onSnapshot` listeners). Both map snapshots through `normalizeTimestamps`, so every document arrives with ISO-string dates.
- **Backend callable shape:** every callable follows one skeleton — auth check, permission check, validate, mutate, audit, respond. Written out in [functions/CLAUDE.md](functions/CLAUDE.md).
- **`REGION` is `asia-southeast2` (Jakarta)** — set in both `functions/src/lib/admin.ts` and [src/services/firebase/functions.ts](src/services/firebase/functions.ts). These must match or every call fails with `NOT_FOUND`.

### RBAC

Three layers, and they are not interchangeable:

1. **Cloud Functions are the enforcement layer.** `requireActiveUser` re-reads `users/{uid}` and `roles/{roleId}` on every call, so permissions come from live Firestore, never from the caller's token. `requirePermission`/`requireAnyPermission` gate the rest.
2. **`firestore.rules` gates reads only** (every collection is write-denied). Rules read `request.auth.token.role/departmentId/outletId` — custom claims kept in sync with the user doc by `functions/src/auth/syncUserClaims.ts`. Reading claims off the token rather than a `get()` keeps rule evaluation cheap, but it means a role change only reaches the rules after the claim syncs and the client refreshes its token.
3. **The client is UX only.** `AuthProvider` ([src/contexts/AuthProvider.tsx](src/contexts/AuthProvider.tsx)) listens via `onAuthStateChanged`, then holds a live `subscribeToDocument` on `users/{uid}` and loads `roles/{roleId}.permissions` into the Zustand auth store. `ProtectedRoute`/`RoleRoute` ([src/routes/](src/routes/)) and `PermissionGuard` read from that store — they hide UI, they don't secure anything.

### Shared "engines"

`functions/src/shared/` holds the cross-module engines every feature builds on: **approval** (`submitApproval`, `approveStep`, `rejectStep`, `returnForRevision`, `cancelApproval`, plus the `onApprovalRequestResolved` trigger), **tasks** (`createTaskInternal` and its callables), **notifications** (`sendNotificationInternal`, `notifyUsersByRole`), **file storage** (`createFileMetadata`/`deleteFile`, soft-delete only), and **calendar**. Prefer calling the `*Internal` helpers from another function over duplicating their logic — they own the audit and notification side effects.

### Frontend structure

Feature-first under [src/features/](src/features/)`<module>/` (pages, components, hooks, services per feature). The router ([src/routes/routes.tsx](src/routes/routes.tsx)) mounts unbuilt modules as `<ModulePlaceholder>`. `src/components/ui/` is a small shadcn-style primitive set (`components.json`, `class-variance-authority`, `tailwind-merge`); `src/components/shared/` is app-level shared components. Global state is Zustand stores in [src/store/](src/store/) (`authStore`, `themeStore`, `toastStore`, `uiStore`). Path alias `@/` → `src/` (configured in both `vite.config.ts` and `tsconfig.json`).

### Intentional duplication

`functions/` is a separate npm package with its own `tsconfig.json`, so it cannot import from `src/`. `functions/src/lib/collections.ts` and `functions/src/lib/permissions.ts` are hand-mirrored subsets of `src/constants/collections.ts` and `src/constants/permissions.ts`; both files say so in their header comments. Role→permission rows live in Firestore `roles/{roleId}` docs, not in any repo constant. When you change a collection name or permission string, **update both copies** — and remember `firestore.rules` hardcodes collection names and role names a third time.

### Frontend conventions worth keeping in mind

- UI is built from the primitives in `src/components/ui/` plus `src/components/shared/EmptyState`. **There is no Dialog/Modal** — multi-step or confirm flows are separate routed pages, not modals. `Button` has no `asChild`/Slot support — use `useNavigate()` + `onClick`, not `<Button asChild><Link>`.
- Static `/new`-style routes must be registered before `/:id` param routes in the same children array to avoid the param route swallowing them.
- Gotcha: a `const` narrowed by an early-return guard (`if (!source) return ...`) does **not** stay narrowed inside a closure (event handler, callback) defined later in the same component when the variable came from `useState` or `.find()` — TS re-widens it to possibly-`undefined` inside the closure. Extract a plain local (`const id = source.id`) right after the guard and reference that inside the closure instead of the original variable.

## Definition of done

A change is finished only when all of these hold:

1. `npm run build` passes at the root with **zero errors**. The tree is green; keep it green.
2. If you touched `functions/`: `npm --prefix functions run build` passes. It is outside the root `tsc` project, so a root build being green says **nothing** about whether `functions/` compiles.
3. If you added or renamed a collection or permission: update both `src/constants/` and the mirror in `functions/src/lib/`, and add the collection's `firestore.rules` block — a new collection with no rules block falls through to the deny-all match and reads silently fail.
4. Any new callable follows the canonical skeleton — `requireActiveUser` → `requirePermission` → validate (`AppError`) → mutate with `newDocumentBaseFields`/`updatedFields` → `recordAuditEvent` → `successResponse`, wrapped in `try/catch (error) { handleError(error) }` — is exported from `functions/src/index.ts`, and the client reaches it only through `callFunction`.
5. Behavior implemented from a spec carries a doc-section citation comment (`// API.md §7` style), matching the existing convention.
6. Any new query with an equality filter plus a range or `orderBy` on a different field has a matching entry in `firestore.indexes.json`. Firestore fails these at runtime, not build time, so the typecheck will not catch it.
7. You exercised the affected flow — `firebase emulators:start` plus `npm run dev` with `VITE_USE_FIREBASE_EMULATOR=true` is the safe way; there is no frontend test suite to lean on.
8. This file's "Current state" section is updated if your change altered any fact in it.
9. Commit messages follow Conventional Commits going forward: `type(scope): summary` (e.g. `feat(hr): ...`, `fix(approval): ...`, `docs: ...`). History before July 2026 is mixed — match the convention, not the history.

## Gotchas

- **A new collection with no `firestore.rules` block is unreadable, not open.** The deny-all `match /{document=**}` at the bottom catches anything that falls through, so the symptom is an empty list or a permission error in the console, not a security hole.
- **Missing composite indexes fail at runtime only.** Firestore rejects an unindexed equality+range/orderBy query with an error containing a URL that creates the index — but add it to `firestore.indexes.json` rather than clicking through, or it won't exist in the next environment.
- **`REGION` must match on both sides.** `functions/src/lib/admin.ts` and `src/services/firebase/functions.ts` both hardcode `asia-southeast2`; a mismatch surfaces as every callable failing with `NOT_FOUND`, which reads like a missing function rather than a config error.
- **Custom claims lag the user doc.** `syncUserClaims` updates the token claims `firestore.rules` reads, but an already-issued ID token keeps its old claims until it refreshes (~1h, or on a forced refresh). Cloud Functions are unaffected — `requireActiveUser` re-reads Firestore every call — so a role change takes effect for writes immediately but for reads only after the token turns over.
- Missing `VITE_FIREBASE_*` vars throw at module load from [src/services/firebase/config.ts](src/services/firebase/config.ts), listing exactly which are absent. If a deployed build fails on sign-in, check the deployment's env vars (e.g. Vercel project settings), not just `.env.local`.
- `strict` TypeScript is on for both the frontend and `functions/`; both builds fail on any type error.
- `npm run lint` fails with "eslint is not recognized" — eslint is not in `devDependencies` and there is no config file, even though the `lint` script itself references real eslint flags. `npm run build` (tsc + vite) is the quality gate until lint is actually scaffolded.
- `generateAppraisalInsights` needs an `ANTHROPIC_API_KEY` secret (`functions/src/lib/secrets.ts`, set via `firebase functions:secrets:set`). Without it the callable deploys but fails at call time.
